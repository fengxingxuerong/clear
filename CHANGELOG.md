# 更新日志 · QuAiWei
> 完整版本历史，细节以对应 Git 提交为准。

## v0.9.15 更新（呈现层：本地分与外部通道分分歧时界面必须说出来）

**背景**：v0.9.14 §22 与 `docs/external-judge-findings.md`（2026-09-22）已经证明一件事——
本地 `aiScore` 在书面体上**系统性低报** AI 味：5 份深度模式交付稿里 4 份本地 ≤13 而外部席位 ≥75，
最刺眼的政务报告是**本地 2 / 外部 90**。但界面上用户看到的只有本地分和「降幅 −40」，
没有任何东西告诉他这个 2 分在外面不成立。**结论躺在文档里，没落到界面上**，等于没有结论。

**做了什么**：新增 `src/engine/score-conflict.ts`（纯函数、零依赖、零 API 成本），
`BenchmarkPanel` 在「本地代理分」那一行下面渲染两级横幅。三个外部通道都接上：
LLM 评判 / 外部检测器 / 朱雀手动回填框（去味后若配了检测器会自动送检，所以这条提示**不需要用户多点一次**）。

**判据（阈值全部标注实测来源，没有拍脑袋的）**

| 常量 | 值 | 依据 |
|---|---|---|
| `CONFLICT_EXTERNAL_AI_MIN` | 60 | 实测分歧稿外部最低 **75**（闭环保姆那条 **73**），留 13pp 余量；再往下取会把官方「疑似 AI」区（30~60）也算进来，那是误报 |
| `CONFLICT_GAP_MIN` | 30 | 一致的那份（D0 本地 8 / 外部 15）只差 **7**，分歧稿最小差 **60**；取 30 落在中间，离两边都远 |
| severe 的本地侧 | `AI_SCORE_HUMAN_MAX`(27) | 复用引擎已有常量，**不另设一套数**——本地分进了人写带却外部判 AI，才是"本地分在低报" |

两级不是同一件事，不合并、不共用一种颜色：
- **severe（红）**：本地分在人写带内、外部判成 AI → 文案直接写「这一篇请以 XX 为准，不要用上面的降幅判断安全性」；
- **warn（黄）**：两把尺差得远但本地也没说自己干净 → 只说"请以 XX 为准"，**不提"低报"**（有测试钉这条）。

**刻意不做的三件事**（都写进模块头注释，免得下次被"顺手补上"）：
- **不改分、不否决交付**。本模块不知道外部通道准不准（LLM 当检测器本身有偏差），没资格替用户决定信谁。
- **只报"外部比本地更像 AI"这一个方向**。反方向（本地 90 / 外部 20）是保守方向的错，
  后果只是多改一轮，不会骗出安全错觉。
- **不做"未送检也警告"**。拿不到外部分就是没有比较对象，那是另一个功能，混进来只会变成常驻噪音。

**测试**：新增 31 条（新模块 27 + 面板渲染 4），`vitest` **799 → 830**（46 文件）。
（基线 799 是本版改动前的 HEAD 实测值：v0.9.14 文末记的 782 之后，9-22 那三个提交又加了 17 条
而没回写到这里——数字按实测写，不沿用旧值。）
新模块的第一组测试就是那 5 份实测真值（逐条断言 severe / 不报），
外加两条**阈值守卫**：把门槛抬到 74 会漏掉"闭环保姆 73"那条、压到 7 会误伤 D0 那份一致稿——
以后有人调阈值，这两条先红。
**红绿验证**：把 `CONFLICT_EXTERNAL_AI_MIN` 从 60 改成 74，实测 **3 条立刻红**
（真值表那条 + 阈值守卫 + gap 边界），恢复即 27/27 绿。

**桌面产物**：已按 v0.9.15 重打（`resources/app` 183.5 MB / 整包 485.3 MB），
`verify-pruned` 五节全绿、退出 0；指纹章 `dirty=false`、`HEAD=5405b2f`。
顺带修了两件会让这道检查**永远不可信**的杂事：
- `scripts/sync-dist.mjs` 的 dirty 判定排除 `.workbuddy/`——它是工具数据目录、每次都在，
  不排除的话每次打包都盖 `dirty=true`，「别拿去发布」的警告天天响，最后一定被忽略。
- `rebuild-app.cjs` 依赖拷贝加逐包打点（耗时 + 体积）。本机实测 `fs.cpSync` 会在某个包上
  **静默卡住**（27 分钟零进展、无报错），此前只能看到「[2] 重建 resources/app」干等；
  有了日志卡点一眼可见。修好后同一次重跑 **1 分 1 秒**跑完

### v0.9.15 追加（暴露链：三件「引擎有、用户拿不到」的能力接上线）

上面那节是呈现层。同一版里另做了三项纯接线改动，起因是一次优化空间勘查——
查出三个「README/CHANGELOG 里已经宣称、代码里却根本没有入口」的洞。

**1) CLI 接上 LLM 通道（`scripts/humanize-cli.ts`）**
此前 CLI 只 import `humanize` + `detectAI`，`src/api` 那 28 个模块（深度闭环 / 交叉评判 /
定向修订 / 长文分块）它一个都够不到——批量这个卖点等于只兑现了本地引擎那一半。
现在 `--api` 走 UI 同款的 `runHumanize`，回退逻辑一并继承；新增
`--base-url/--model/--api-key/--judge-model/--alt-model/--no-deep/--contest/--max-calls/--max-wait/--strict/--persona/--report`，
`--seed` 也不再硬编码（此前写死 20260905，批量结果无法与 UI 对齐复现）。
Key 优先级：`--api-key` > `QUAIWEI_API_KEY` > `OPENAI_API_KEY` > `loadPresetKeys()`。
两个 CLI 专属的坑已拦在入口：baseUrl 以 `/` 开头（同源代理，CLI 无代理必挂）、拿不到 Key。

**2) UI 有文件导入/导出了（`App.tsx`）**
此前只有「粘贴进 → 复制出」，改一篇三千字的东西要先从编辑器复制、改完再粘回去。
新增「导入」（.txt/.md，≤2MB，FileReader 读文本）与「导出」（Blob 下载 .txt，带日期时间戳）。
**.docx 仍未做**——需要引入解析库，README 竞品表里那一行继续标 ⏳。

**3) 自定义保护术语有入口了（`term-protect.ts` + `SettingsModal`）**
`setProtectedTerms()` 从 v0.8.6 起就只有 `term-protect.test.ts` 在调，而 `CHANGELOG` 明写
「用户可通过 setProtectedTerms() 扩充自定义术语」——承诺没兑现。现在设置面板有
「自定义保护术语」输入框（换行或逗号分隔，单条 ≥2 字），存 localStorage，挂载时与保存时
各注入一次引擎（保护集是模块级全局，不注入刷新就只剩内置 58 项）。

**测试**：`vitest` **830 → 836**（新增 6 条：store 术语持久化 3 + 设置面板上抛 1 + CLI 参数 2）。
**踩到的坑**：把 CLI 的 `main` 改成 `async` 后，`scripts/scripts-logic.test.ts` 挂了——
它用 `indexOf("function main")` 切源码片段塞进 vm 沙箱，切片尾部残留裸 `async`
（ReferenceError: async is not defined）。已改为正则匹配 `(async\s+)?function main`，
并把起点前移到 `const USAGE`（parseArgs 现在引用这个模块级常量）。

### v0.9.15 追加二（测试补盲四连：836 → 919；仓库首次公开上线）

本批没有引擎行为改动，全是测试与工程安全。起因是一次全栈体检 + 仓库首次推送公开 GitHub
（`fengxingxuerong/clear`，master + tags）。

**推送前的历史清洗（安全前置）**：全历史 blob 扫出 3 条仍在用的 SenseNova 明文 Key
（34792ea 起的历史泄露，详见 `docs/key-rotation-status.md`），用 `git filter-repo`
把 blob 与提交消息里的字面量统一替换为 `***REMOVED***`；重写前打了全历史 bundle 备份。
清洗后复扫：blob / 提交消息 / 跟踪内容三路 `sk-` 形态 **0 命中**，tags 保留，hash 全变属预期。
**注意：这 3 条 Key 仍然有效**（还活在另外两个项目的 `.env` 与审计报告里），轮换 runbook 依旧成立。
gitignore 同步补 `.env` / `.env.*`（保留 `.env.example` 白名单）与 `.workbuddy/`。

**四轮测试补盲（836 → 879 → 892 → 912 → 919）**：

- **第一轮（ppl 双宿主 + 结构级行为锁，+38）**：`scorer-core.ts` 补 `maskGroups` 与
  `maskedMeanNll`（手算 log-softmax 对拍 / 多目标均值 / 大 logits 数值稳定）——语句覆盖
  73.7%→**100%**；`ppl-client.ts` 新建测试（mock Electron 桥与 Worker：透传/抛错回落/
  桥不完整回落、握手/error 事件/progress 转发/构造失败可重试、窗结果过滤聚合）——
  77.5%→**97.5%**；`humanize-shuffle.ts` 锁 breakSummaryTail / injectSelfQA /
  dismantleExpositionTrilogy / classifyExpositionScore / preDetectHumanFingerprint
  五个此前 0 测试的结构级函数——74.3%→77.0%。
- **第二轮（公共 API 行为锁，+13）**：`crossChunkCleanup`（垫词去重 / 标点封顶 /
  句后连接词硬剥离 / CJK 空格 aggressive vs 打字空格尊重）与 `mechanicalShuffle`
  （空文本 / 同 seed 确定性 / 内容守恒 ≥80% / humanHand 钳制 / dialogue 场景块头保真 /
  intensity 0 轻管线）——77.0%→79.5%。
- **第三轮（结构层编排，+20）**：`structuralShuffleParagraph`（朱雀 P3 触发词清除 + 内容守恒 +
  总分总拆段）、`hardNumberedEnumerationShuffle`（三条编号改造路径 + 概率分支）、
  `enforceParagraphLeadSentVariance`（prependShort / chopLead）、
  `boostBurstinessIfLow`（锚封顶 CAP=2 / 已有锚不新增 / 相邻复读折叠 / 场景块保护）——
  79.5%→**88.7%**。
- **第四轮（P3-4 编造红线，+7）**：`injectFirstPersonAnchorPoints` 专项——数量契约
  `max(1, floor(chars/300))`、DATA_MARK 优先槽位、**锚点句必须全为第一人称主观看法池成员
  且不得声称具体经历/资历/内部资料**（把「严禁编造铁律」在此函数上钉成测试）。

**覆盖率总账**：全局语句 84.7→**87.9%**、分支 78.1→**80.9%**、行 86.0→**88.9%**；
`check:release` 六段式门禁每轮复跑均 **exit 0**。

**顺手记录的两个已知偏差（未修，原因见下）**：
① `hardNumberedEnumerationShuffle` 头注释宣称支持「1. / (2) / 第三： / ①」四种编号，
NUM 正则实际只干净匹配「二、」式——「1.」replace 残留标点、「第三：」「①」完全不匹配；
② `boostBurstinessIfLow` 的场景块保护只在多段分支生效，单块场景文本（无空行分隔）
会被注入语气锚（真实剧本走多段格式不受影响）。
**两者都动了就会改引擎输出字节 → 触发 aiScore x 轴漂移**（漂移棘轮已压满 6/18、Δmax 35/35），
修复必须与「新官方点位重拟合四条体裁线」一并评估，不许单点糊过去。偏差现状已在测试里
用注释 + 用例钉住，未来修复时测试会红提醒同步评估。

**测试补盲续作（六、七轮：919 → 954，仓库同步 GitHub `fengxingxuerong/clear`）**：

- **六轮**：`zhuque-semantic` 新建测试（71.8%→**97.4%**）——缓存命中/绕过重跑/模型签名隔离
  （缓存绝不串味）/交叉模型文案/50 条 LRU 淘汰/score 钳制；`SettingsModal` 条件分支
  （66.7%→70.0%）——baseUrl 相对路径部署警告横幅、文风切换草稿-上抛、Key 草稿态不落盘。
- **七轮（长尾清扫）**：`humanizeBestOf` 边界（空候选跳过/三重门槛淘汰/**全部淘汰回退单次
  生成**/seed 可复现）；`term-protect` 用户术语切片前后文拼接保护；`store-history`
  坏 JSON/非数组/10 条截断/配额溢出逐条裁剪与放弃持久化；`BenchmarkPanel` 五档建议文案、
  `ZhuquePanel` 语义权重滑块。
- **环境坑落档**：happy-dom 下 `vi.spyOn(localStorage, "setItem")` 会触发 Worker native 崩溃
  （node::OnFatalError，fork 直接消失）——本地存储 mock 一律改直接赋值覆盖 + finally 还原。
- **总账**：体检基线 836 → **954**（+118）；全局语句 84.7→**89.3%**、行 86.0→**90.4%**、
  分支 78.1→**82.3%**；最弱文件清零（scorer-core 100%、ppl-client 97.4%、zhuque-semantic
  97.4%、shuffle 88.7%、App 48.7%）；七轮每轮 `check:release` 均 exit 0。

**真网关端到端实测（CLI --api 暴露链首次实弹，2026-09-25）**：

v0.9.15 接线的 CLI LLM 通道此前只有单测没有真网关实弹。本轮拿 SenseNova `kimi-k3`
（`token.sensenova.cn/v1`）跑通两轮：

- **单轮（--no-deep，2 次调用）**：本地 AI 分 **67→22**（12.5s），`report.json` 的
  `engine:"llm"/usedApi/roundScores/degrade` 与 UI `runHumanize` 返回结构一字不差。
- **深度闭环（--max-calls 6）**：2 轮评分 58→62，"第 2 轮未优于当前最优，停止后续轮次"
  未改进即停守卫生效；评判锚点放宽（首轮 58 → 达标线 ≤20）、字数压缩率等提示全链可见；
  最终 67→24，交付最优稿。
- **抓到一个坑顺手修了**：深度模式 `--max-calls 6` 太紧，第 2 轮质检被预算闸掐断，
  `catch {}` 把根因吞掉只报"质检通道异常"——现在放行提示带错误摘要（截断 120 字），
  下次直接可见是预算耗尽还是网关故障。max-calls=9 重跑验证：预算超扣可见（"已达上限
  9 次（实际 11 次）"），质检不再异常。
- **使用建议**：kimi-k3 是思考型模型（单轮 12~150s），深度模式 `--max-calls` 建议 ≥9，
  否则质检会被预算掐断。

**v0.9.16：.docx 导入导出（零依赖）**：

README 竞品表最后一项 ⏳ 清零。`src/docx-io.ts` 用运行时内置的 CompressionStream /
DecompressionStream + 手写 zip 读写器（~200 行）实现「docx ↔ 纯文本」，不为一个功能拖 3MB
解析库进打包产物。导入：提取 word/document.xml 段落（<w:p>/<w:t>/<w:br/>，表格单元格按段，
XML 实体还原）；导出：生成最小合法 OOXML（Word/WPS/Google Docs 可打开）。诚实边界：
加粗/公式/图片等富文本格式不保留——去味引擎只吃纯文本。App：导入框收 .docx、新增
「导出 .docx」按钮；docx 解析失败时把实际部件名报给用户。CLI 的 .docx 输入暂未接（下版）。
11 条新测试含 round-trip 自证与 zip 结构/CRC 校验；顺手修掉 zip reader 的 EOCD
条目数偏移读错（+6 应为 +8，自产自销测试抓的）。

## v0.9.14 更新（深度模式：编造否决 + 首轮好区收手 + 定锚与日志归属修正）

**背景**：v0.9.13 之后拿真网关跑了四篇（s1 议论文 / s2 种草文 / s3 技术科普 / s4 财报短讯，
合计 70 次调用、27.4 分钟）。通路是健康的（交付稿数字全部保真、无塌句），但测出四个要修的点。

**1) 编造从"警告"升级为"拒绝交付"（`strictFidelity` 开启时）**
实测 s2 交付稿含三处原文没有的断言：原文"重量仅为450克"→"**裸机**450克"、
"完全满足日常需求"→"**出门一天**够用"、原文无第一人称→"**我觉得**算省心"。当时收稿终审
抓到了，却只往 note 写一句"请人工核对"，稿子照样交付——营销文里凭空多出的技术参数和时长
断言是要发出去错的。
现在终审发现编造即抛 `FabricationVetoError`，由 `runHumanize` 回退本地引擎并标 degrade
（本地引擎只做换词/删套话，结构上不具备新增事实断言的能力，是这里唯一干净的兜底）。
上层文案把"被拒收"和"通路故障"分开写：degrade/note 说「编造否决」，不再混成"API 调用失败"。
默认 `strictFidelity=false`，本否决只影响显式开启严格保真的用户。

顺带补了一个不对称：收稿终审此前**只在主循环收场那条路径上跑**，"竞争段首轮就达标"的稿子
反而不过终审。现在两条返回路径统一走 `deliver()`（终审 → 达标线说明 → 未达标提示）。

**2) 首轮落在人写带即收手，不再开修订轮**
`effTarget() = max(绝对目标, 首轮×0.35)` 的注释写着"首轮已很低（≤28）→ 维持绝对目标不变"，
但这句话从未落到公式上，而且"维持绝对目标"本身就是继续往下追：s4 首轮 15 分，目标算出来
仍是 10，于是再开一轮。
现在首轮 ≤ `JUDGE_GOOD_BAND`(28) 时目标取它自身、就地收手。**不放宽绝对目标**——首轮 8 分
那例目标仍是 10（v0.8.8 原测试的 `targetUsed=10` 与 `roundScores=[8]` 两条断言一字未改）。

**措辞纠正**：本版早期写作"修订轮 2/2 负收益、把稿改坏"，不准确。实测是**两轮修订都没拿到
更优分（0/2 收益）**：s4 的修订稿 49 高于首轮最优 15、s1 的修订稿 92 高于 89，两次都被守卫拦住没有接管交付
（46 是 s4 竞争段的第二个候选稿，不是修订结果），
所以代价是**白烧一整轮调用**而不是交付质量下降（唯一一次修订稿真被交出去的是 s2，那是
上面第 1 条编造问题）。收益也**不写成"调用减半"**：省下的是该稿的整条修订链，而"减半"需要
同输入各 ≥3 次真跑才能折算，本版没做。

**3) 竞争段定锚口径修正（本版实测发现的真缺陷）**
竞争段按候选序号抢先 `anchorScore = res.score`，锚的是**首个过检分**，与同文件 v0.8.9 注释
写明的"锚点取最优（最低）分而非首个"相反。实测场景（同输入两候选判 46 与 15）：
锚到 46 → 目标 `round(46×0.35)=16` → 15 分稿反而"不达标" → 又白开一轮修订——正是本版
第 2 条要省掉的那种轮次。改为由循环后的 `bestScore`（各有效分最低值）定锚。
取证方式：在当前代码上**单独**把那一行恢复回去，测试即红 `expected 16 to be 15`；恢复即绿
（单变量，其余保持本版）。顺带纠正一处口误：这个目标线是 16，不是最初讨论时写的 17。

**4) 质检结论按稿归属（修日志误报）**
`qcIssues` 原来是随轮次滚动的变量，谁最后失败就报谁。实测 s3 交付的是过检稿，日志却报着
被淘汰候选的两条"谓语丢失"——逐字核对交付稿并不存在（原文整句被压成"大语言模型成了…话题
之一"，主谓完整）。改为 `Map<稿文本, 该稿自己的清单>`，只报真正交付那一稿；`pass=true` 但
非空的项（如"质检通道异常放行"）照实保留，不被后续轮清空掩盖。
（曾有误读说这两条"谓语丢失"属于 s1：grep 四份日志，"谓语"只在 s3 出现 2 次、s1 为 0 次。）

**5) 方差警告（后续任何 A/B 的前置条件）**
同一篇 s4 两次真跑：首轮胜出分 **15 与 48**、调用 **20 与 15**；而 s3 的 0.6 档历史上判过
16/24/38，本日 pro 判 46。**评委分跨日不可比**，所以本版的每一条收益都锁在打桩层断言上
（首轮在人写带时改写调用 `2→1`、终审否决的接线、定锚单行回退即红），不是拿一次网关耗时算的。
后续 A/B 必须先固定样本集与重复次数（每边 ≥3）。

**5) 编造复核的两处静默失效补掉**
`fabricationReview` 原来只读 `r.content`，而 `chat()` 把正文与 reasoning 分开返回、不合并
（`qualityCheck` 早就在用 `content || reasoning`，见 `llm-quality.ts:52`）。真网关跑 s2 时
确实抛过一次"未返回有效 JSON"（原始响应没截到，不能断言就是 reasoning-only）；但两种后果都不该
由一次解析失败决定：候选期抛错=静默丢稿，终审抛错=静默放行。现按既有约定补兜底，并且——
更关键的是——**终审没跑成时写进 note**：「⚠️ 编造复核未能完成…本次未做否决——交付稿未经事实核查」。
静默跳过会让用户以为稿子过了核查，而它根本没查。degrade 文案也加了"复核有漏判率，不担保干净"。

**6) 一个反例，记下来免得下次过度承诺**
拿 s2 那份含"裸机450克"的稿子直接调复核：glm-5.2 与 deepseek-v4-flash **都判 0 项编造**；
而真跑那次的终审抓到过同一处。也就是说同一把尺子双向都不稳（既漏判也偶发误报），所以
"否决"是尽力而为的闸门、不是担保；也因此**没有**去做"否决后退回次优候选"——次优候选过了
同一条复核并不能证明它更干净，只是多赌一次同样的尺子，反而不如退回本地引擎（本地不新增事实）。

**未修（下一步，数字已量）**：`maxApiCalls/maxWaitSeconds` 只在轮间/候选前检查，候选内部
（复验→修复→再复验→复核→评判）不检查。假时钟探针（每次调用推进 10s、首候选判 FAIL 走完整
修复链）实测：越过 deadline 后还会新起 **4 次**调用（无严格保真）/ **6 次**（开严格保真）/
**8 次**（竞争 2 候选 + 严格保真），即最多超 45~80s；按本网关真实单次 25~35s 折算，
最坏可超 **2~4 分钟**。s2 实测 340s vs 300s 上限即此机制的表现。
**决定不收紧**：中途丢弃半成品候选等于把已经花掉的 3~6 次调用全部作废，还更容易落到本地稿——
比让它跑完当前阶段更差。本轮真正改掉的是**到点后仍继续发起下一次尝试**：`chat()` 现在收调用方
传入的 `overBudget`，只掐 429 换 Key 与 2+5+12s 退避的重试链（首个请求照发，交付语义不变），
并新增 `BudgetStoppedError` 把「预算到点」与「通道故障」分开——被 catch-all 混成后者的话，
未核查的稿会被当成「质检通道异常，放行」交付出去。收稿终审**不传** predicate：安全核查不能被
时限静默取消。以上写进 README 的「最长等待是软上限」条。
两处红绿验证：摘掉 `chat` 里的检查点即红（fetch 从 1 次变 3 次）；若把 predicate 也传进终审，
note 就会冒出「编造复核未能完成」——新增的收场口径用例正是为了拦住这个设计错误。

**一次公开撤回**：中途我曾据「候选内部不检查」推断存在「429 退避风暴无上限」，实测不成立——
`backoffs = [2000,5000,12000]` 且受 `attempt < backoffs.length` 约束，`chatNonEmpty` 也早有
`i > 0 && isOverBudget()` 断点；100×50ms 的假响应只会把预算内的下一次改写推过线约 2s，不会
无限循环。相关说法已按实测改掉，探针文件用完即删，不再拿它撑结论。
另外长文停在 87~90 说明该改的是改写提示词/模型选型，不是再烧一轮修订。

**7) aiScore 的打分边界导成引擎常量并加刻度守卫**
27 / 29 / 30 这三个数此前只存在于 gitignore 的标定草稿和测试文件里，引擎侧没有导出常量——
**调权重、加检测器时没有任何东西会拦住你**。现在 `humanize-metrics.ts` 导出
`AI_SCORE_HUMAN_MAX`(真人实测上限 27) / `AI_SCORE_MACHINE_MIN`(污染实测下限 30) /
`AI_SCORE_CUTOFF`(判定线 29)，标定测试的 `CUTOFF` 改为导入。
新增守卫刻意比"分离性检验"更严：分离性只要求两组不重叠，**整条尺子一起平移时它照样绿**；
守卫把两组样本钉在标定带内（2026-09-19 实测量在 humanMax=13 / machineMin=32），
任何一次权重改动把真人稿推过 27 或把污染稿压到 30 以下都会红。摘掉验证过：把带界收到 12
即红（`真人稿最高分 13 被推过人写带上限 12`），恢复即绿。
顺带把 `JUDGE_GOOD_BAND = 28` 的注释改准：那是**评委尺度**上的收手点，与上面三个数不是同一
量纲，只是数值靠近、容易被误读成同源。

**8) `regression-12samples` 从"永久红"改成能用的门禁：漂移与退化分开拦**
这道门禁在已提交状态上就是红的（A 3/12），红到没人看，等于没有。根因是**尺子拿错了**：
A 套件比的是 `calibration-data-v2-genres.json` 里**冻在 git 里的文本**——那段文本的生成过程
根本不参与本次运行，能动它分数的只有评分器本身，所以它天生测不出引擎退化。实测存档的
`对话·0.6` 记 6 分、同一段文字今天重打 100 分，差 94 分，而这一段里没有任何东西调用过 `humanize()`。
（生成脚本 `scripts/archive/gen-calibration-texts-v2-genres.ts:95` 确实是现算现存，所以 6 分
在它自己那天的刻度下是真的——刻度后来整体换过了。）
另两个含糊点顺手挑出来：A 里 `论说 O2/O3` 的"存档文本"是脚本现场用**当前引擎**算的，
一行里混了引擎和评分器两个变量；A 的 `O1 原文` 基线取 `aiScore(EXPO_O1_RAW)` 自身，
所以那一行永远恒等、永远绿，是个空位。

改法：按"被打分的那段文本冻不冻在仓库里"分两组。
- **D 评分器漂移**（10 段冻结文本）：D1 绝对刻度守卫只比对上一项导出的 `AI_SCORE_HUMAN_MAX`/
  `AI_SCORE_MACHINE_MIN`，不看任何历史数字（人写原文 ≤27、三条 AI 原文 ≥30、分离度 ≥20，
  HEAD 实测 33−7=26）；D2 方向棘轮按每条的 `want` 只许朝好方向走，**slack=0**——`aiScore`
  纯确定性，坏方向的位移不可能用"测量噪声"解释。
- **E 引擎退化**（8 档当前引擎同 seed=20260826 产物）：只降不升棘轮，基线在锁文件里。

三条防自欺，每条都有测试打穿（新增 `regression-12samples.test.ts` 15 例）：
① 锁文件缺失只会红，**不再自动初始化**（旧 `loadOrInitLock` 会把今天的输出直接写成明天的真值）；
② 有真退化时 `--rebaseline` 拒绝写锁（只缺记录的首轮建线例外，否则永远起不了步）；
③ `--rebaseline` 必须带非空原因否则退出码 2，原因与逐条差值追加进锁的 `rebaseLog`，
脚本对锁文件平时只读不写。`--lock` / `--sep-min` 两个自检开关就是为了"能证明它会拦"才加的：
`--sep-min 27` 实测退出 1（把分离度门槛抬到实测之上），`drift.H0=6`/`e2e.N1=3` 喂进去也都退出 1。
`calibration-data-v2-genres.json` 里那批老 `aiScore` 数字一个字未动，仍在文件里可查。

顺带把 `npm run test:regress12` 补进 `check:release`（此前这条门禁只在 CI 的 yml 里挂着，
本地一键发布检查根本不调它）。

**9) 桌面版 Key 池绕开 safeStorage 明文落 localStorage（P1 秘密存储）**
核对《使用说明》时撞出来的。桌面版一直宣称"API Key 走系统级加密落盘"，代码确实有这条通道
（`electron-app/main.js:107` 起，safeStorage 加密写 `userData/secure-config.json`），
但**加密的字段只有一个**：
  · `App.tsx` 收口时写 `saveApi({ ...a, apiKey: "" })`，只抹单 Key `apiKey`；
  · Key 池 `apiKeys` 原样进 `saveApi` → `localStorage.setItem(K_API, JSON.stringify(c))`；
  · `store.ts` 的 `loadApi` 还专门把 `apiKeys` 读回来（注释记着"v0.9.5 修复此前漏读导致
    刷新后 Key 池丢失"——那次修复把池子接进了 localStorage 明文这条路，方向是对的、落点是错的）；
  · `main.tsx` 启动只从安全存储取回 `apiKey` 一个字段；
  · 而 `SettingsModal:165` 点「填入 SenseNova 常驻通道」写的**正是** `apiKeys`，
    `effectiveKeys() = splitKeys(apiKey + apiKeys)` 又把两者合并使用。
净效果：唯一会承载多个可用 Key 的那个字段，整条加密通道对它无效，明文躺在 userData 里。

修法：`store.ts` 补齐 `loadApiKeysSecure/saveApiKeysSecure`，把"存哪儿"的决定从 `App.tsx`
收进 `persistApiConfig()`（这一步同时决定 Key 会不会丢、会不会漏，放在组件里就没人测得到），
`main.tsx` 走 `adoptSecureApiKeys()`；两个函数都**只在确认写进加密存储之后**才抹明文。
最后这条不是形式主义：`main.js` 在 `safeStorage.isEncryptionAvailable()=false` 时返回 false，
原先调用方把返回值丢了——所以"无条件抹明文"在这里等于**销毁用户的 Key**，
那是拿数据安全的名义制造数据丢失。现在这种情形退回明文（与修复前一致，不会更差），
如实返回 false，并写进《使用说明》。
`adoptSecureApiKeys()` 顺手做升级迁移：老桌面版留在 localStorage 的明文池，
下次启动自动搬进加密存储再抹掉，不用等用户再保存一次设置。Web 版返回空对象，
启动路径逐字节不变（有测试锁这条）。

红绿验证（`src/store.test.ts` 17 → 25 例）：把 `saveApi({...a, apiKey:"", apiKeys:""})`
退回成只抹 `apiKey`，`localStorage 里搜不到任何 Key 片段` 立刻红
（`expected '{"enabled":false,…' not to contain 'sk-POOL-A-0002'`）；
把 `if (okMain && okPool)` 改成恒真，`safeStorage 不可用` 那条也立刻红。两处均已恢复。
另：`main.tsx` 里那 15 行迁移逻辑一开始写在 bootstrap（无测试覆盖、且含销毁数据的分支），
已挪进 `store.ts` 并逐条配上断言。

**顺带修掉两份《使用说明》里的过期承诺**（都是逐条对代码核过的）：
  · `electron-app/使用说明.txt` 头部停在 **v0.8.2**，而这份文件正是 `electron-dist/` 里
    实际随 exe 发出去的那份（与产物内文件哈希一致）。"14 特征"实测是 16（`detector.ts` 数得）；
    "内置 3 Key 轮换"**对桌面版根本不成立**——`loadPresetKeys()` 只读 `SENSENOVA_KEYS` 环境变量
    或 `scripts/.sensenova-keys`，两者都不在打包产物里，按钮会置灰显示「（未配置 Key）」。
    界面是诚实的，文档在骗人。已按实测重写，并补 v0.9.14 三条行为变化与迁移说明。
  · 根目录 `使用说明.txt` 头部停在 v0.9.5，补到 v0.9.14 并加上好区收手 / 软上限 / 严格保真三条。
核过没改的：`DEEP_MAX_ROUNDS=4`、攒 4 条出官方分估计 / 8 条启用留出验证（`calib-lab.ts:355`）、
`DEFAULT_LOCAL={bestOf:true, candidates:8}`（store.ts:19）、样本 D 两点锚点 99.99/98.47。

**10) 凭证账本的 append-only 从"自觉"变成会拦的门禁**
账本（`evidence/zhuque/ledger.jsonl`）整套价值都在"历史行不会被动"，`seal` 也确实只 `appendFileSync`。
但 `audit` 查的是**字段对不对得上**，三类被动它看不见：整行被删（少看一条，剩下的一致就报通过）、
行被重排（它不关心顺序）、改不参与复算的自由文本字段如 `note`（复算不到，静默通过）。
新增 `scripts/check-ledger-appendonly.ts`，判据只取最强的一条：**HEAD 的每一行必须按原顺序
原样出现在新内容的开头**，只许往后追加——删除/改写/重排在"最长公共前缀断裂"这一步一律现形，
不需要维护第二套规则（删一行和改一行在前缀断裂上本来就是同一个现象，硬拆成两种报错只会给读者假精确）。

接线三处：`.githooks/pre-commit`（只在 `git diff --cached --name-only` 命中账本路径时才跑，
普通提交不付 tsx 启动开销；命中即阻止并提示"要更正就追加一条新的，让 pct-conflict 去暴露矛盾"）、
CI 新增 `Ledger append-only` 步骤（顺手补上此前只在本地跑的 `Evidence audit` 步骤）、
`npm run check:ledger` 并入 `check:release`。
**今天这条在 check:release/CI 上是平凡通过的**（账本 0 行、覆盖率 0/18），它要到第一笔 `seal`
入账才开始承重——写清楚是为了不让人误以为它已经在保护什么。

测试 `scripts/check-ledger-appendonly.test.ts` 19 例，逐条造一次篡改打穿：改一个字节、
只改 `note`、删中间一行、重排、截断尾部、整本清空、"先删一行再追加一行"（最像正常修订的那一种），
以及反向的三条不误伤（纯追加 / 零追加 / HEAD 本来就空的首次建账本）+ CRLF 与尾随空行不算篡改。
git 取数那两路（`git show HEAD:<path>` 与索引 `:<path>`）另在临时仓库里端到端跑一遍——只用文件对照测
的话，"HEAD 读不到""索引里才是待提交版本"这类接线错误会全躲过去。
hook 本身也在临时仓库实测三种形态：纯追加放行、改写历史行拦下（退出 1，且发生在类型检查之前）、
完全不碰账本时这道检查根本不运行。

写这一版时自己踩到一个"看着接了线其实没接"：`parseArgs` 里 `--head-file/--staged-file` 只做了
非空校验、忘了把值赋回 `o.headFile/o.stagedFile`，于是文件对照分支永远进不去、静默回落到真仓库的
git——按 import 跑测试会全绿而测的完全是另一条路。已修，并补一条断言：`--head-file` 指向不存在的
文件时按"无历史"处理且 `--repo` 指到一个不存在的目录也必须退出 0（真回落到 git 就不成立）。

**11) docs 与代码的系统对账（做法：机械抽引用逐条查存在性，不靠眼力）**
今天已经三次靠运气撞到"文档承诺与代码不符"（桌面版《使用说明》的"内置 3 Key 轮换"、
README 的 82→30、`~3~5ms`），所以做了一次全量：把 `docs/*.md` + README + 两份《使用说明》里
所有 `代码段` 抽出来分类做存在性核对（npm script 名 / 文件路径 / 符号名 / 目录），
首轮 209 处可核对引用，flag 出 9 处、其中 **6 处是真缺失**（另 3 处是探针误判，见文末）。
顺带查出更要紧的一条**归因错误**（不在引用核对范围内，是读上下文时撞见的）：

- **`docs/fingerprint-and-zhuque-calibration.md` 声称「此后任何引擎改动导致校准漂移都会在 CI 亮红
  并强制同步重拟合」，并把功劳记在 `zhuque.test.ts` 的 ≤8pp 锚点守卫上——那个守卫测不到引擎漂移。**
  它用的是 `calibration-data.json` 里**存档的 x**，全程不调 `humanize()`，守的是"拟合线 vs 存档数据"；
  引擎把 x 推动 35 分它也照样绿（今天 calib-sanity 正报着 Δmax 35 而 vitest 715 全过，就是并存证据）。
  真正测漂移的只有 `calib-sanity` 的 x 轴重算棘轮，而它是"只许变好"的棘轮、当前 6/6 点与 Δmax 35
  **两项都正好压在天花板上**，再多一点才红。已按实测更正归因，并写明"亮红之后该做什么仍只靠人"。
  注：那段里的拟合数字本身不必改——它们本来就是对存档 x 的拟合，自洽；`zhuque.test.ts:228-229`
  的代码注释也一直写对了，错的只有文档。
- 5 个已归档脚本被按活路径引用：`zhuque-calibration.txt` / `zhuque-v2-fit-12pt.ts` /
  `zhuque-calibration-v2.txt` / `api-test.ts` / `demo-drop.ts`（均在 `scripts/archive/`），
  另 `recheck-calib.ts` 全仓已不存在（一次性体检脚本）——改为写明"当时用一次性体检脚本发现，
  同类检查现在走 `npm run test:calib`"。归档的拟合脚本还补了一句：**它们读存档 x，跑出来不是当前引擎的 x**。
- `zhuque-gap-analysis.md` 写 `detector.ts` "14 特征"，实测 16（与桌面版《使用说明》同一个错）。
- `judge-anchor-calibration.md` 的「首轮已低（≤28）→ 维持绝对目标」正是本版第 2 条修掉的那句空话，
  改为记录 v0.9.14 的实际行为（就地收手、绝对目标没放宽），并补上"评委分跨日不可比 ⇒ 28 是保守点位不是标定阈值"。
- 同文「相关文件」把 4 条 `artifacts/` 路径当可复跑资源列出，而 `artifacts/` 已 gitignore——
  本机确实在，干净克隆拿不到。已标注。（探针的 `dir-path` 分支没覆盖 `artifacts/` 前缀，这条是人看出来的。）

- 「诚实声明」里那句"实测自带分 13~22，而独立指标实测仍在 41~59"两个数**没标样本来源**，容易被读成守恒结论。
  现按能查的半句重测（回归套件同源文本、seed=20260826、`detectAI.probability`）：原文 22~48 → 去味后 **19~23**，
  同批 aiScore 从 7~46 → **0~8**。没有拿这组去替换原数字——原那组来自另一批真网关实测稿，样本不同，
  换数等于用一个没核过的数盖住另一个；改成两个数各标自己的出处，并写明**13~22/41~59 那批的独立指标
  未在当前引擎复测**。"自带分低 ≠ 过得了检"这个结论不受影响。

**没有把探针提成常驻门禁**，理由就是本版第 8 条在治的病：剩余 4 处"缺失"里 3 处是探针自己的误判
（`userData/secure-config.json` 是 Electron 运行时路径不是仓库路径；`scripts/zhuque-evidence.ts seal`
是代码段里带了一个参数；两处 `demo-drop.ts` 是**有意**在讲历史并已在同句标注归档位置）。
一个常态红或者需要白名单的门禁，下次就有人 `--no-verify`，那和没有一样。用完即删，做法记在这里。

**12) `verify-pruned` 新增"随包文档/版本一致性"，当场查出现有产物是 v0.9.0 时代打的**
给产物校验器加第 [3] 节之前并不知道有问题：本版刚把 `electron-app/使用说明.txt` 从 v0.8.2 修到
v0.9.14，而 `electron-dist/QuAiWei-win32-x64/`（当前唯一现存的桌面产物）里那份文档**逐字节还是旧的**，
`resources/app/package.json` 更是 **0.9.0**、仓库根是 0.9.14。也就是说这个产物整体落后 14 个 minor，
而 `verify-pruned.cjs` 一路报"✅ 全部通过。依赖链完整，可以发布。"——它只查模块能不能加载，
从来不比对"这份产物是不是从当前代码树上打的"。
**同时构成对前一处结论的更正**：本版早前我据"`npm run build` 退出 0 + `prune-dist --dry-run` 485.3MB
+ `verify-pruned` 全部通过"报过"生产构建与产物健康"。其中 verify 那半是在这个 v0.9.0 产物上测的，
它对"当前代码能否发布"没有证明力；`npm run build`（Web dist）那半仍然成立。

补法两层：
  · `verify-pruned.cjs` [3] 节逐字节比对源与随包《使用说明.txt》、比对 `resources/app/package.json`
    与仓库根 version，任一不符即 `failed++` → 退出码 1、末行改为"不要发布"。失败信息直接写清该跑哪条命令。
  · `rebuild-app.cjs` 新增 2b 步：把《使用说明.txt》刷到交付目录根。原来这一层只有
    `electron-packager` 会写，而 `repack` 才是日常重建路径——**文档因此永远不会被日常路径刷新**，
    这正是它能停在 v0.8.2 的机制原因（用户读的是目录根那份，不是 `resources/app` 里那份）。
  · 顺带把末行从"先恢复被误删的文件"改成按失败类型分别给指引——新增的两类失败不是"文件被删"。

实测红绿：加检查后对旧产物立刻 2 红（`落后于源` + `版本不一致：产物 app=0.9.0 而仓库根=0.9.14`），
按提示跑 `npm run build` → `npm run repack` 后 [3] 两项转绿、整体退出 0。
刷完的产物里 `resources/app/package.json = 0.9.14`、随包文档 `QuAiWei v0.9.14`、
主 chunk 含 `apiKeys` 5 处（即本版第 9 条 Key 池修复确实在交付的 bundle 里，不只是源码里）。

**13) 在真浏览器里点了一遍 UI，当场抓到三处"交付文本是病句"**
起因很简单：前面所有结论都是拿脚本喂引擎读分数，没人真的**看过界面输出**。
用 Chrome DevTools Protocol（Node 24 自带 WebSocket，不引 playwright）跑「示例 → 去味 →
AI 检测 → 指纹体检 → 朱雀检测 → 设置 → 窄屏」全路径，界面本身没问题（按钮可达、
禁用态正确、390px 无横向溢出、常驻通道按钮如实置灰并写明原因），但**输出文本里有病句**，
而 `aiScore` 对这几处一律给中位 6 分（它只看词与结构，看不见坏标点和搭配错误）。

拿 App.tsx 里那段 SAMPLE_TEXT 跑 40 种子 × 2 档 = 80 次实测：

| 缺陷 | 修前 | 修法 | 修后 |
|---|---|---|---|
| 标点相撞 `，。`（…推动可持续发展，。） | **40/80 = 50%** | 第 6 步兜底挪到管线**最后** | 0/80 |
| `非常提高了` 一类搭配病句 | **30/80 = 38%** | 从 `极大地` 替换表里去掉「非常」 | 0/80 |
| `特别提升了` 一类搭配病句 | **21/80 = 26%** | 同上，去掉「特别」（只留 大大/明显） | 0/80 |

**① 标点相撞是顺序错误，不是缺规则**：`humanize()` 第 3 步本来就有
`.replace(/，。/g,"。")`，但排在它后面的 `stripAICliches` 删掉句尾套话
（「具有十分重要的意义」）后留下悬空的「，」，接上原句句号就是「，。」——
归一跑在了制造者的**前面**。已在句长节奏兜底之后补第 6 步（只收全角相撞形态，
不碰 `injectHalfWidth` 掺的半角「,」，两条策略不互相抵消）。
讽刺的是 `humanize-shuffle.ts:1460` 的注释早就写下过「拉动可持续发展，。说到底」这个病例，
修在了 shuffle 自己的出口，主路径上同样那句话还是漏了。

**② 替换表本身有语法错误**：`humanize-vocab.ts` 的 `极大地: ["大大","特别","非常"]`——
「非常/特别」不能修饰带「了」的动补结构。同文件紧邻两条注释（"靠着/拿"、"陆陆续续"）
说明这个坑以前踩过两次，只是这一条没进同一套审查。换成「大大/明显」（带不带「了」都成立，
且都不在 `humanize-metrics` 词表里，所以不引入新扣分项）。

**③ 一处我自己的测量错误，公开更正**：第一次探针把"句末悬空连接词"写成 3%——
因为正则只匹配了「以 随着 开头的句子」，而真输出里那句已被改成「说起来，…」开头，
判据当场失效却看起来像"测过了"。按正确判据重测 120 次，实际是 **99/120 = 83%**：
「综上所述」被换成「归结起来」后**挂到了前一句的句尾**（「…AI 写作工具跟着就来了，归结起来。」），
是这三处里最显眼、也最高频的一个。**没有顺手修**——它出在 shuffle/重切分的重组环节
（`breakSummaryTail` 整句搬移 / `splitOnConnectors` 重拼 之一），要二分定位才能确定，
而 shuffle 正是标定线与 D/E 棘轮最敏感的地方，盲改的代价是把刚建好的棘轮当试错场。

**顺带**：dev 首页缺 `/favicon.ico`（浏览器每次请求一条 404），未修，属独立小项。

红绿验证：把第 6 步整块删掉，新测试立刻红在
`expected '有个细节，AI 写作工具跟着就来了，归结起来。数字化办公明显提升了…' not to match /[，、]。/`；
恢复即绿。新增 2 条回归（`humanize.test.ts` 34→36）跑 40 种子 × 2 档断言零相撞标点、
零「非常/特别 + V + 了」。我一开始还写了第三条，但它断言的是个根本不可能出现的形态
（`,.`）且把自己算出来的信号 `void` 掉了，属凑数，已删。
实测这次改动**没有松动 D/E 棘轮**（`test:regress12` 无"可收紧"提示，D 10/10 · E 8/8 原值通过）。

**14) 编造的本地防线：增殖硬否决 + 新增内容披露（零 API 成本）**
承接第 13 条的 UI 实测。s1 那篇深度产物量出来是 **391 → 977 字（250%）、第一人称标记 0 → 16**，
而 README 第 107 行早就写着「输出字数不超过原文 110%」——**这条铁律此前没有任何测试或运行时守它**。
长度膨胀是"模型补内容"最确定的代理信号，且完全免费，所以它该是本地守卫，不该等 LLM 复核。

先测分布再定阈值（**没有照抄 110%**）：
  · 9 份真 LLM 深度产物（s1~s7，`out-deep` / `out-phase2-deep`）长度比 = 58% 63% 83% 83% 90% 95% 96% 109% **113%**
  · 本地引擎 225 次（3 体裁 × 3 档 × 25 种子）最大 **100%**（它只换词不增词）
  · 异常样本 = 250%
所以：**150% 硬否决**（离合法上限留 37pp 余量），**110~150% 只在 note 里提示**。
拿 110% 当否决会直接误杀实测里那两篇 109%/113% 的合格稿。

第一人称/口语具体化计数**只做披露、不做否决**，理由是数据不支持：合法样本里第一人称增量最高就有
**+10**（s5-长文分块.run.txt），异常样本是 +16，n=9 上根本分不开——硬设一条线就是拍脑袋误杀。
但它必须让用户看见：现有的编造兜底 `FABRICATION_SIGNALS` 是**亲属/熟人短语表**
（我朋友/我同事/我舅/我妈…），永远追不上模型花样——**s1 那 16 处第一人称一个都没命中**。
结构性计数补的正是这个缺口，且放在 `strictFidelity` **之外**：不开严格保真的用户同样该知道
"交付稿凭空多出十几处『我』"。

**被既有测试当场抓出一个真缺陷**：接上后 `llm-quality.test.ts` 立刻红两条，原因是夹具原文只有
5 字、mock 改写 21 字 = 420%，比例守卫把"短句被补全成完整句"当成增殖打了回。
**这不是测试该改，是我的判据缺规模下限**——比例类守卫在小样本上没有意义。
改成三条同时成立才否决：`原文 ≥60 字` 且 `>150%` 且 `绝对增量 >60 字`（s1 异常样本三条全中：
391 字 / 250% / +586）。并把这条教训单独钉成 3 个测试，防止下次又写一个无下限的比例守卫。

新增 10 个测试（`vitest` 717 → **727**）：250% 必拦、113% 不得误杀、必须真接在 `localHardGate`
上（防"只定义不接线"）、本地引擎产物一律不得触发（参照系）、+16 第一人称必报警 / +1 不得报警、
110~150% 只提示、以及上面那三条规模下限。
README 的 110% 那段同步改写清楚：它是**给模型的目标**不是硬线，并明确标注
"这些守卫只保证没凭空变长/没多出经历标记，**不保证事实正确**；真否决仍要开严格保真（默认关，
因为每候选多花一次调用）"。

**门禁**：`tsc --noEmit` 0、`eslint src/ scripts/` 0、`vitest run` **727 通过 / 44 文件**（上一版记的 673 之上 +54：回归门禁自测 15 + Key 存储 8 + 账本 append-only 19 + 病句 2 + 增殖/编造守卫 10；
其余为 好区 3 / 归属 2 / 否决 4 / 定锚 1 / 上层接线 2 / 复核兜底 2 / 预算停止重试与收场口径 3 / 刻度守卫 1。
除真网关那批观测外，每条断言都做过红绿验证——把对应改动单独退回即红）、`scan-bugs` 0 违规、`verify-quality` 通过、
`regression-12samples` D 10/10 · E 8/8 · 刻度守卫 ✅（本版首次在当前代码上绿）、`calib-sanity` 通过（6/6 点、Δmax 35——**两项都正好压在天花板上**，
下次引擎再动刻度就会红，那是 T16 重拟合欠的账，不要把天花板再抬高一格混过去）、`check:ledger` 通过（当前平凡）、`check:release` 退出 0。
`llm-humanize.ts` / `llm-quality.ts` / `humanize-metrics.ts` 未跑 prettier：这三个文件在本次改动之前
就不 clean（实测 `d018910` 版同样 warn；CI 无 prettier 步骤），`--write` 会连带重写没碰过的旧代码，
故只对齐自己改动部分；新增/改写的测试文件（`llm-chat.test.ts`、`humanize-metrics-calibration.test.ts`）
已按格式收齐。

**15) 朱雀官方 API 已开放：真值闭环从"手点网页"变成"只差一个 Key"（2026-09-20）**

网页版送检这条路当天就撞死了：脚本一点「立即检测」就先弹腾讯滑块（`captcha.gtimg.com/static/template/drag_ele`），
不去解、也没解，**额度一次没花**；而 s2 深改稿 199 字 / 原文 345 字，两个版本都够不到网页版 350 字门槛。
但只读抓官网 DOM 时发现 `<a class="api-entry">使用API</a>` 指向
`cloud.tencent.com/document/product/1552/137539` —— 朱雀**是有官方 API 的**，
仓库里「朱雀没有公开 API，官方结果只能走网页」（`src/api/zhuque.ts` 头注释、`ZhuquePanel` 界面文案、
README 对标段）和「EdgeOne 网关（企业版）」（`BenchmarkPanel`）四处说法全是过期的。
文档口径逐条核过（拉原始 HTML 验字符串确实存在，没有只信摘要）：EdgeOne Makers 内置模型
`@makers/zhuque-text`，固定网关 `POST https://ai-gateway.edgeone.link/v1/providers/zhuque-text/classify`，
Bearer Key，body `{text, is_merge}`（默认 true = 整篇一个分，与网页口径一致），分数在
`softmax_confidence`（0~1，越大越像风险内容），`labels_ratio` 另给三档占比，
额度按 `makers_models_usage.total_tokens` 扣（不是 `usage.total_tokens`），50 万 token/月免费，仅文本。
**不需要自建网关**——预设按钮此前拿 `your-gateway.edgeone.app` 占位域名拼路径，也一并换成官方固定域名。

顺带堵住一处方向最错的 bug：`scoreViaDetector` 原来只挡「路径取不到数」，
HTTP 200 + `{"status":"error","msg":"quota exceeded","softmax_confidence":0}` 会被读成
**0 分 = 完全人类**，一次配额耗尽会被记成一次完美的去味成功。现在 `failureSignal` 只在响应
**自报失败**时开火（status 非成功词 / msg 非空 / error 非空），且三类信号全部拼进错误消息留现场；
不做"成功白名单"式的收紧，没有 `status` 字段的第三方接口行为不变。

接线：`detector.ts` 新增 `ZHUQUE_OFFICIAL_DETECTOR` 预设与 `classifyViaDetector`（保留原始响应，
`scoreViaDetector` 改成它的投影，出网路径只留一条——请求逻辑分两处写，守卫就只在一处生效）；
`scripts/zhuque-api-score.ts` 批量 CLI，Key 只从环境变量读（命令行会留在 shell 历史，写文件会被 git 看见）。
真发一次未授权请求验通链路：`401 {"error":{"code":"auth_failed"}}`，扣 0 token。
两个失败分支实测退出码 2（缺 Key）/ 1（送检失败），过程中还修掉一个 Windows 陷阱：
`process.exit()` 在 fetch 句柄未关完时炸 libuv 断言，把真实退出码盖成 127，改用 `process.exitCode`。
预设也不再替用户勾「启用检测器」——点按钮只填口径，开不开（=花不花外部账号额度）由用户决定，
旧测试断言 `enabled=true` 已同步改成断言 false。测试 727 → **736**（+8：200-但自报失败的四种形态、
正常响应不误伤、无 status 向后兼容、非对象 JSON、预设口径、文档真实样例跑分）。`check:release` 退出 0。

**16) 修掉「句末悬空连接词」病句：切分器不再靠造光杆孤句抬 CV（#24 / #27）**

`boostBurstinessByCutting` 的 v0.9.2 插入语分支注释写着"放宽区间仍走全套守卫，不会切出残句"，
代码却是 `mid = firstComma` 一刀裸切、**一个守卫都没走**。「总的来看，数字化办公…」被切成
4 字光杆孤句「总的来看。」，下游 `fixOrphanConnectiveLeads` 再把它向左粘回上一句尾部，
产出「…AI 写作工具跟着就来了，总的来看。」。实测命中率：UI 示例文本 102/280 次运行（36.4%），
scan-bugs 的外部 3 样本 × 2 强度 × 30 种子里 **79/180 次（44%）**。
**而这 180 次运行此前一条红都没有**——scan-bugs 根本没有这个签名，`collapseIssues` 也不抓它。

修法按"修在制造者、不修在清理器"：切点一律落在插入语**之后**（把插入语并入前半句），
且走 `findGuardedCutNear` 的全套守卫（括号深度 / 状语残句 / 光杆谓语 / 量词同位语一票否决）。
上一轮在清理器里做的尝试已记在案：那个函数在管线里跑得比切分**早**，看不到孤句，
只会把病句换成另一种坏形态（孤立 4 字句），这次不重蹈。修复后两处复现口径都归零。

**代价算过账，没有藏**：那条分支唯一的"贡献"就是靠孤句抬 CV，去掉之后
`scan-bugs` 的「句长节奏过平」计数 31 → **41**（CV 落在 0.31~0.39，不是塌到 0.05 那种），
`regression-12samples` 的 E 组 O2 1→2、O3 0→1（各 +1 分代理分，都还在人写带 ≤27 内）。
这两项都是**已实测证伪的弱指标**（v0.9.6：人写口语随笔 CV=0.27 是全场最低、判别力≈0 且方向反），
拿它换 44% 的病句率方向明确。计数基线在 `scan-bugs.ts` 里带完整理由改写，
棘轮则是走新加的 `--accept-worse "O2,O3"` 指名放行、差值与原因进 `rebaseLog`——
不是手改锁文件，也不是"红了就放宽"。

**顺带补的门禁缺口**：`scan-bugs.brokenSigs` 新增「句末悬空连接词」硬拦（旧代码下 79 次命中、
新代码 0 次，红绿都实测过）；`humanize-shuffle-guards.test.ts` 新增 8 条函数级断言
（3 个插入语 × 3 个目标 CV + 1 条"别把分支一关了之"的反永真式哨兵），其中 3 条在旧代码下红。
`regression-12samples` 新增 `--accept-worse`：必须点名 id、必须配原因、名单与实际红灯不吻合即拒绝，
配 3 条自测（放行 / 不吻合仍红且不写锁 / 缺名单退 2）。
另修 `classify-genre.test.ts` 一条**靠缺陷成立的断言**：它原来比 humanHand@0.9 与 普通@0.48
"输出不同"，而那点不同正是本分支多切的一刀；钳制后两条路径本就是同一配置，
现在改断真机制（与 main@0.9 必须不同 + 0.9/0.48 两档必须逐字相同）。

测试 736 → **750**（+14），`check:release` 退出 0。

**17) 顺着同一条线又抓出一个病句：枚举标记被单独收成一句（32.9%）**

修完 #27 回头看交付文本，发现同一段里还有一句更怪的：「…可以从以下三个方面**入手：首先。**
企业需要加大研发投入；」——「首先」两个字独占一句。UI 论说样本实测 **69/210 次 = 32.9%**，
强度 ≥0.5 起。

根因不在列举处理，而在切点守卫 `fragmentFrontCanStand` 取"末段"时只按 `，、；` 切、**不含冒号**：
「具体来说，可以从以下三个方面入手：首先」按逗号切出来的末段是 5 字的「入手：首先」，
刚好躲过"≤4 字且无谓语标记即否决"这道门槛，于是切点被判为安全、句号化后成了光杆句。
修正是分隔符字符类加冒号（`[，、；：:]`），一行。修复后 69/210 → **0/210**。

误伤检查：冒号后是完整小句（「…入手：趋势已经很明显了」）照常放行，
逗号路径的原有否决不变——三条都写成测试（`fragmentFrontCanStand` 此前**一条直接测试都没有**，
而它是所有切分 pass 共用的那道闸）。`scan-bugs.brokenSigs` 同步加「枚举标记光杆成句」硬拦。
`check:release` 退出 0，节奏计数没有因此再涨（仍 ≤41），测试 750 → **753**。

**18) 18 个历史标定点的凭证回收：能补两级，第三级补不出来就明说**

账本 `evidence/zhuque/ledger.jsonl` 一直是 **0 条**——18 个标定点的官方百分比只活在
`calibration-data.json` 的数字和档案正文里，克隆到别的机器上什么都验不了。
先做取证（不猜，逐点查档案）：

- **L2 官分出处：18/18 都在**，分散在三份档案里（v2 回传行 / v3 TSV 官分列 / v0.7 档案正文对照行），
  且与正本 y **逐点一致**，没有一处对不上；
- **L1 送检原文：10/18 还在**——9 篇内联在 `zhuque-manual-inputs-v2-genres.txt` 的 id 分块里
  （档案同时记了字数，抽出来重数**逐点相符** 452/476/523/652/691/765/364/386/446），
  O1 另有 `EXPO_O1_RAW.txt`；剩下 8 个（v3 六篇引用的 `.txt` 已不在仓库、v0.7 两篇是旧引擎产物）文本已失；
- **L3 页面截图：0/18。** 当年没人截图，这一级**伪造不出来，也不该伪造**。

于是账本加了显式分级字段 `proof`（`screenshot` / `text+transcript` / `transcript-only`）+
`proofSource`（必须写到"文件:行号"），`scripts/zhuque-retro-backfill.ts` 把现存的东西收回来：
10 点到 `text+transcript`、8 点到 `transcript-only`，原文归档进 `evidence/zhuque/retro/`，
`audit` 每次重算 sha256 并与账本比对——事后改一个字就是 `hash-mismatch` 硬伤。

**关键是别让它变成自我加冕**，三处专门焊死：
① `audit` 把非 `screenshot` 级的点**继续**报 `no-evidence`，`--strict`（`check:publish`）照旧红
（实测：非严格退出 0 但打印「截图认证 0/18」，严格退出 1、18 项硬伤）；
② 原来那句「凭证覆盖率：18/18 个标定点有凭证」是**错的表述**——回填完它就报满覆盖，
换成按级打印；③ 新增 `proof-mismatch` 硬伤：`proof=screenshot` 却没截图、有截图却把级别写低、
回填级不写出处，三种都拦。测试 753 → **761**（+9：回填带文本 / 无文本 / 出处不写到行号即拒 /
官分与正本不符即拒 / 手改成 screenshot 级冒充→硬伤 / 重复回填幂等而内容不同拒绝覆盖 /
strict 下回填不算绿 等），`check:release` 退出 0。

过程中自己踩到并修掉的两个：`transcript-only` 的 `submitFile` 是空串，
`path.resolve(base, "")` 指向**仓库根目录**，`readFileSync` 直接 EISDIR 把整个审计炸掉；
以及 `sealRetro` 的官分正则我少写一个捕获组、把 `m[3]` 当值读成 NaN，dry-run 里 3 个点"官分 NaN"才暴露。
另外这台机器上 `zhuque-evidence.ts` 是 **CRLF** 行尾，多行 Edit 会静默不匹配却报成功——
第二次撞到同一类坑，改单行替换 + 事后 grep 复核才落对。

**19) CI 里那道 append-only 检查在结构上不可能失败——修好了**

回答"还有什么要优化"时顺手查 CI，撞到一个能证明的缺口：`.github/workflows/ci.yml` 里
`npx tsx scripts/check-ledger-appendonly.ts` 不带参数，而默认模式比的是 **HEAD vs 工作区**——
CI 是干净检出，两者永远相等。实测：`HEAD 18 行 → 待提交 18 行 / ✅ 只追加了 0 行`，
**这一步无论仓库里发生过什么都不会红**。`actions/checkout@v4` 默认 `fetch-depth: 1`
也拿不到任何基准 ref。也就是说：账本历史不可改这条保证，此前只靠本地 hook，
而 hook 恰好是 `--no-verify` 能跳过的东西——正是本项目一直在治的那个病。

修法：脚本加 `--head-ref <ref>`，CI 显式给基准（PR 比 `origin/<base_ref>`，push 比
`github.event.before`）并把 `fetch-depth` 设 0。两个刻意的设计：
- **ref 解析不出来 → 退出码 2**，绝不退化成"当作没有历史"而假绿（浅克隆/拼错 ref 都会红）；
- 首次推送（`event.before` 全零）确实没有基准，这时**打印 `::warning::` 说明本轮不覆盖任何东西**
  再退出 0，而不是假装查过了。

红绿都实测过：临时克隆里把某条历史行的 `officialPct` 改一个数字并**提交**，
`--head-ref <旧 SHA>` → 退出 1 且打印两版现场；同一次改动走默认模式 → 退出 0（漏网，
这条对照写成了测试，免得以后有人把它当"检查过了"）。另加 5 条测试覆盖
ref 有效/ref 不存在/给了 flag 没给值/基准下账本尚不存在。测试 761 → **766**。
`npm run lint`（CI 有、我这轮此前一直没跑过）0 错，`check:release` 退出 0。

**20) 桌面产物落后 7 个提交而自检全绿——补一条"产物来自当前源码"的指纹断言**

上一轮我修的是《使用说明.txt》停在 v0.8.2 那件事，加的检查是"产物内版本号 == 仓库根版本号"。
这次发现**那条检查自己就是个漏洞**：版本号要等发版才动，而代码天天在改。实测——
产物建在 `2026-09-19 18:18`，之后 `src/`+`scripts/` 又走了 7 个提交，里面**还带着当天刚修掉的
两个病句（外部样本 44% / 32.9%）和「检测器故障被读成 0 分 = 完全人类」那个 bug**，
而 `verify-pruned.cjs` 照样报「✅ 全部通过，可以发布」。它比的是 `0.9.14 == 0.9.14`。

**版本相等从来不等于来自当前代码树。** 新加的是文件级指纹：
`electron-app/build-fingerprint.cjs` 按组哈希真正进产物的输入（`src/` 全部、前端入口与配置、
Electron 主进程脚本、随包《使用说明.txt》、`scripts/sync-dist.mjs` 本身），
共 103 个文件；`verify-pruned` 的 `[4] 新鲜度` 重算一遍并**按组点名**哪里变了。

设计上有两处是成败关键，都写进了注释：
- **章必须在 `npm run build` 时盖，不能等 `repack` 时算。** 否则"只 repack 不 build"会把
  **新**源码的指纹配着**旧** bundle 记进产物，这条断言当场失效。所以盖章落在 `sync-dist.mjs`
  （build 链最后一环），`rebuild-app.cjs` 只负责搬运，且**缺 `build-info.json` 直接退出 1**。
- **覆盖范围刻意窄。** 改 `scripts/` 下的门禁、`docs/`、`evidence/` 不该让产物判过期——
  天天误报的检查最后一定被人 `--no-verify` 掉，那正是本项目一直在治的病。

红绿双向都实测过（探针跑完按原字节还原文件）：
改 `src/engine/humanize-shuffle.ts` → 退出 1 且点名 `src`；改 `docs/…md` 或 `scripts/scan-bugs.ts`
→ 仍退出 0（不误报）；改随包《使用说明.txt》→ 退出 1 且点名 `electron-main`；
旧产物（没有 `build-info.json`）→ 退出 1。`dirty` 标记也验到了：本次重打时工作区有未提交改动，
产物如实打出「这份产物不对应任何一次提交，别拿去发布」。
重打后产物里能 grep 到 `检测器未给出分数` 与 `ai-gateway.edgeone.link`（都是今天才有的字符串），
内容层面确认修复真的进了包，不只是指纹说它进了。

`electron-app/` 在 eslint 的 ignores 名单里（CJS 配浏览器/TS 规则本就不合，HEAD 那份
`verify-pruned.cjs` 单文件 `--no-ignore` 也报 32 条），所以这里改用最有意义的静态检查：
四个文件全部 `node --check` 通过。

**21) 回填脚本的 id 映射补上自证：档案自己声明的体裁必须等于正本 genre**

`zhuque-retro-backfill.ts` 把档案里的裸 id（N2 / D1 / H2…）映射到正本带版本的 id（N2v2 / D0 / H2v2…），
其中 **`D1 → D0` 这类是推断**（档案那块自己写着"对话体 / 原文"，正本管那个点叫 D0）。
映射错了会怎样？—— 文本照样归档、哈希照样对得上、`audit` 照样通过，因为 audit 只验
"文件与账本一致"，它不知道哪段文本本该属于哪个点。而那 18 条记录现在是标定地基。

原有那道官分比对（档案 pct 必须等于正本 y）能挡住大多数错位，**但挡不住两个点官分恰好相等的那种**。
所以补第二条独立信号：档案声明的体裁必须等于正本 `genre`（v2 块头是中文「对话体」、
v3 TSV 列本来就是英文枚举，两种都得认——第一版我只认中文，六个 v3 点全被误判"不在映射表里"，
dry-run 当场现形）。

跑完的结果顺带把那次推断证实了：**15 个带体裁声明的点（v2 九块 + v3 六行）逐条与正本一致**，
0 拒收；v0.7 那 3 点档案正文里没有体裁声明，只能继续靠官分。

新增 `scripts/zhuque-retro-backfill.test.ts` 10 条，其中一条是**专门为"体裁检查不是装饰"写的**：
把 `N3` 故意错挂到 `H2v3` 上——两边官分都是 18，官分那道闸完全无感，只有体裁（叙事文 vs humanHand）
能发现。反向实验：把体裁检查关掉，**恰好这一条红**，其余 9 条照绿；恢复后 10/10。
另有"映射表不许出现重复目标"（两个档案块抢同一个正本 id 是隐性覆盖）和 dry-run 不写盘。

测试 766 → **776**（45 文件），`tsc`/`lint`/`check:release` 全 0。
指纹不受影响（`scripts/` 不进产物），所以不用重打桌面包。

**22) 深度模式真机重测（40 次调用）+ 补上 LLM 通道唯一的结构守卫（#30）**

先记一次**我自己仪器错归因**：上一版测试脚本误沿用 2026-09-15 旧矩阵的
`maxWaitSeconds:300 / maxApiCalls:8`，于是 N1 在 419 秒撞等待上限、回退本地引擎。
那测的是我的配置，不是产品 —— 产品默认是 `0/0` 不限制（`llm-config.ts:185`）。作废重跑。
另外第一版用的样文里 **6/8 篇本地分本来就 ≤12**（没东西可去），换成
`evidence/zhuque/retro/` 里三篇**有转录官分**的 AI 原文（99% / 98% / 85%）重测。

结果（3 篇 / 40 次调用 / 24 分 8 秒，全表见 `docs/deep-mode-evidence-audit.md` §七）：

| 输入 | 交付路径 | 闭环达标 | 评委(AI味) | 本地(AI味) | 长度比 |
|---|---|---|---|---|---|
| N1 99% | **回退本地** | 否 | — | 33→8 | 99% |
| D0 98% | LLM | **是** | 6 | 46→8 | **52%** |
| O1 85% | LLM | 否 | 92→73→88 | 43→13 | 78% |

**唯一"达标"的那篇是靠删掉一半内容做到的**：`【场景：一家创业公司的会议室…】` 整行消失、
说话人职务标注 4→0（`张总（项目经理）：`→`张总：`）、652→336 字，而闭环评委判它 6 分。
根因是可查的：`isSceneBlockLine` 只用在 `src/engine/*`（本地引擎 P8「块头是元数据不是行文」
整行原样返回），`grep -rln isSceneBlock src/api/` **零命中** —— LLM 通道从来没有对应的那一半。
同一批里 N1 的质检能抓到"丢一个景物词『潺潺溪水』"并拒绝交付，却放行这里"丢一整行元数据"，
说明质检查的是随机抽样细节、不是可点数的结构清单。

补 `structureIssues()` 接进 `localHardGate`（零成本、确定性）：数**场景块行 / Markdown 标题 /
说话人职务标注**三类元素的点数，只数"从有到全无"，不数"变短"。
**刻意划的边界**：列举标记（首先/一是/1.）是引擎主动拆的
（`breakEnumerationStructure`、总结类过渡词直删都是核心去味手段），守卫一旦去数它们就会
和设计打架、把合格稿全判死 —— 这条写成了一个反向测试钉住，并做了红绿验证：
临时把守卫改成"也数列举标记"，恰好那一条翻红，还原后 46/46。
长度阈值 `SEVERE_SHRINK_RATIO=0.4` **没动** —— D0 的 0.52 是合法通过，而 n=3 不足以挪这条线
（项目自己文档记的压缩带是 25~44%，要改得先测出真实分布）。

真数据双向验：D0 原文/交付稿 → 红（两条都抓到）；O1 交付稿、N1 回退稿、两篇原文自反 → 全绿。

顺带记下另外两条没修的：**O1 跑满 3 轮、评委分 92→73→88 从未接近达标线，产品照样交付**，
只在 note 里写一句"可能仍偏 AI"，而同一篇本地 `aiScore` 说 13 —— 两把尺差 60 分且界面上
没有任何机制显示这个分歧（下一个该修的呈现层问题）；以及**病句普查 0/3**：三篇交付稿在
六项成句性检查上全部零命中，这是本轮唯一的正面结论。
测试 776 → **782**（+6），`lint` 0 错，`check:release` 退出 0。

## v0.9.13 更新（标尺错别字项误伤清算 + 官方送检凭证账本）

**背景**：给"外部真值"补流水线（送检凭证账本）时，先撞上一件更糟的事——漂移表里 8 个点的内部分
**恰好等于 60**。一个常量不像是"引擎变好了"，更像是某一项在无条件开火。顺藤摸到标尺的一个假阳性
发生器，它同时污染了 18 个标定点里的 8 个 x 值。先修标尺，再谈凭证。

**1) 错别字项 `/大这?家/` 把正常词「大家」判成错字（P0）**
每处 +30、两处 +60（该项上限），而 `typoCount` 只加进 `score`、不在 `ScoreBreakdown` 里外露——
于是界面和探针都显示"各项命中 0"，分数却是满档，谁也无法诊断。全仓语料扫描：该模式命中 **30 处，
全部是真人写法**（"大家早上好""提醒大家的是""请大家在上线当天"），另外 4 个模式（再去年/再上个/
是实上/时候候）**真实命中 0 处**——即这一项从未抓到过真错字，只在制造假阳性。
收紧为 `/大这家/`（引擎真实造出的错字形态），并把 `typoHits` 加入 `ScoreBreakdown`。实测影响：

| 点 | 修前 | 修后 | 记录值 | 官方分 |
|---|---|---|---|---|
| D2v2/D3v2/D1v3/H1/H2v2/H2v3 等 8 点 | 60 | **0** | 0~2 | 17~28% |
| D0（对话体原文） | 100（顶格） | 46 | 81 | 98% |
| H0（纯人写稿） | 37 | **7** | 10 | 8.33% |

x 轴漂移点 **14/18 → 6/18**。H0 从 37 回到 7 而官方分是 8.33%，这一条不只是修 bug，是标定质量本身变好。
⚠️ 遗留：D/H 两轨的换算线当初就是拿被污染的 x 拟合的，需要用修好的 x 重拟合（等新的官方送检点）。

**2) 顺带纠正一处仓库内的错误归因**
README 与漂移说明把"8 个点 x=0→60"归因于 v0.9.10 清算的 `Number(null)===0` 塌缩锚点污染——**是错的**，
真因即上述「大家」。已按实测改写该段，并保留"这是纠正"的标注。

**3) 官方送检凭证账本 `scripts/zhuque-evidence.ts`（补发布阻塞项）**
18 个官方送检点**一个凭证都没有**：没有送检原文、没有页面截图、没有时间戳，只有人工抄的百分比。
`5/5 判人类` 那条结论的引文在仓库内被反向解释过两次，而没有任何东西能发现这件事。新账本把一次送检
绑成四条互证：送检文本字节哈希 + 截图字节哈希 + 页面原文 + 手抄 pct/label，并加三条设计约束——
- **页面原文参与判定**：`seal` 当场调用引擎同一个 `parseOfficialResult` 解析 `--verdict`，解析出的
  百分比/档位与手填值不符即拒绝入账。只存 pct+label 无效：两者同源于同一次手抄，一起抄反永远自洽。
- **凭证必须复制进已跟踪的 `evidence/zhuque/`**：`artifacts/` 与 `scripts/zhuque-v4-out/` 都被 gitignore，
  哈希指向不入库的文件 = 克隆后什么都验不了。落点还强制在 `--base` 之内（防仓库外文件被抄进仓库、
  防账本存成绝对路径换机器解析不出）。
- **与 `calibration-data.json` 的 `y` 互证**：同 id 官分不符即报错——凭证能反过来指出标定数据抄错了。
档位口径按**官方百分比**（≥60 AI / ≥30 疑似 / 否则人工，`src/api/zhuque.ts`），不是内部综合分口径
（`engine/zhuque.ts` 的 ≥40/≥20），混用会把整条线读严；档位词也统一用官方 `suspected`。
历史 18 点无凭证只列软账不拦（永久红等于没人看，`calib-sanity` 就是前车之鉴），发布前用 `--strict` 升级。
附 46 条用例，每条审计规则都靠"故意造一次假"打穿（改原文 / 换截图 / 删文件 / 手改账本字段 /
git 冲突标记 / 极性读反 / 不足 350 字 / 孤儿 id / strict 退出码）。

**4) `calib-sanity` 加退出码（此前报 14/18 漂移却恒退 0）**
【1】参数一致性与【2】拟合误差是自洽检查，不过即拦；【3】x 轴漂移做成**基线棘轮**（当前 6 点、Δmax 35），
只许变好不许变差，`--ceiling` 可临时收紧自测——实测 `--ceiling 5` 退出 1、默认退出 0。

**5) 新增两道聚合门（此前没有任何一道串起来的发布门）**
`npm run check:release` = test + test:regress + test:quality + test:calib + test:evidence，日常与发版前跑；
`npm run check:publish` = `zhuque-evidence audit --strict`，把"历史点无凭证"也升级成硬伤。
两者故意分档：前者今天全绿（可用作日常门），后者今天**必然红**（覆盖率 0/18）——它就是那条
"没有带截图的官方真值就别对外发布"的实体闸门，不是装饰。

**随版收编**（v0.9.12 之后落地、此前无条目的三项）：多候选赛马改有界并发（实测墙钟 −25%）、
修订轮"字数只减不增"改为等量替换（±10%，机制修正、效果未证）、摘除被跟踪的明文 Key 并给 pre-commit 加密钥拦截。

**门禁**：`tsc --noEmit` 0、`eslint src/ scripts/` 0、`vitest run` **655 全通过**（+51）、
`scan-bugs` 0 违规、`verify-quality` 通过、`calib-sanity` 通过（6/6 基线）、`check:release` 退出 0。

## v0.9.12 更新（LLM 通路反馈回路纠偏：三处自相矛盾清算 + 交付来源可见）

**背景**：v0.9.11 之后把审查对象从本地引擎换到 LLM 通路（`src/api/llm-*.ts`）。结果不是找到几个
孤立 bug，而是同一根因的四个断面——**判定"这稿改得更好了"的那套反馈回路在系统性地骗自己**：
指标只奖励"删掉了"、门槛否决自己刚做的补齐、修好的稿被丢掉、一个布尔说不清交付稿是谁写的。
以下每条都有实测数字，且都补了锁住行为的用例（本版共 +19 条测试，604 全通过）。

**1) 修复稿必须接管交付与评分（P0）**
`processCandidate` 的 `draft` 写成 `const`，修复轮产出的文本只参与重算 `qc` 的 pass 位、
从未被任何变量接住。后果：`return { shuffled: draft }` 交付的是**没过质检**的那一稿，
评委评的也是落选稿，调用方拿 `cand.shuffled` 当最终稿并复查指纹。把修复还原成旧实现跑新用例
即可复现：原文 `营收增长23%`，交付稿返回 `营收增长32%`——正是被本地硬门槛打回的数字篡改稿。
修复轮的输入也从 `content` 改为 `draft`（打回清单判在 draft 上，给模型看另一版文本等于让它
追一个在自己那版里不存在的问题）。修复成功路径此前零覆盖，bug 就藏在这个缺口里。

**2) 空格回填不再被自家硬门槛判死（P0：LLM 通路此前必灭）**
`processCandidate` 先 `restoreMixedSpacing` 补齐中英/中数空格、再跑 `localHardGate`，而
`fingerprintCheck` 第 1 条把这种空格算成硬把柄。实测一版本身**零把柄**的紧排稿，回填后凭空
多出 5 处命中 → 打回 → 修复稿再回填再打回。只要原文带一处这种空格（AI 稿常态："AI 写作"
"GPT-4""1750 亿"）每个候选都必死，整条 LLM 通路静默退回本地引擎，且每候选白烧一次修复调用。
修法选"门槛服从既有策略"而非撤掉回填：本仓库 v0.8.8 已定策"空格完全跟随原文排版"
（`stripCJKEdgeSpaces` 注释原文："既然空格不是可靠的 AI 语义特征……改为完全跟随原文"，
SYSTEM_PROMPT 第 18 条同口径），孤立项是 `fingerprintCheck` 那条老判据。
`STAT_SOFT` 更名 `NON_VETOING` 并收录该项；空格仍保留在用户可见的指纹体检输出里，只是不再
充当否决理由。

**3) 交付稿的真实来源一路可见**
`RunResult` 原先只有 `usedApi` 一个布尔，它回答"有没有调用过 API"而不是"这稿是谁写的"。
两处后果：API 失败回退时界面写「使用本地引擎去味（未配置/未启用 API）」——用户付了调用，
看到的是假话；长文分块单块失败由本地温和档补位时 `usedApi` 仍为 true，混拼完全分不出来。
新增 `engine: "llm"|"local"|"mixed"|"passthrough"` 与 `degrade: string[]`，覆盖全部 4 条回退
分支；`degrade` 前置进 note（note 是 UI 唯一无条件展示的一行）。历史记录同步存 `engine`
（可选字段，无需数据迁移）；旧记录只能按 `usedApi` 让步推断，**永久丢掉了混拼信息**。

**4) 择优补上"成句"一票否决，并把判据变成永久回归网**
`bestOf` 的 `rank = warns*1000 + aiScore.score`，而 aiScore 对"人工智能技术。"这种光杆主语
给 0 分——目标函数在奖励删除。新增 `collapseIssues`：与**原文做差分**，输出小句若是原文某小句
的严格前缀、尾巴被砍 ≥2 字、且那个原文小句在输出中已消失，判为谓语被删光。接入 bestOf
（三重→四重门槛）与 LLM 侧 `localHardGate`，不动 aiScore 刻度故对既有标定零影响。
判据在两处修正才收敛：切句分隔符两侧必须同套（漏全角冒号会整批误报）、且必须要求被截句
已消失（否则 `"字"` 重复串这类嵌套前缀文本整批误杀——误杀会让择优全灭、静默退化成单趟）。
实测 900 次真实引擎输出误报 0，带语气词的"人工智能技术吧。"仍能抓到。
`scan-bugs` 另挂差分探针，罩住签名正则防不住的那一类（当前 180 次运行 0 命中）。

**5) 评委分数行解析收紧**
旧判据 `/^(?:\D*?)(\d{1,3})(?:\D*)$/` 只要"行内一个数字"即算分数，而评委提示词要求先列痕迹
清单再给分、清单里几乎每行都带数字（"残留痕迹2处"），从后往前找正好把 2 处读成 2 分，
系统性压低评分并污染择优。改为严格匹配裸数字行/带标签行（这本就是提示词的明文契约），
解析不出则返回 null 走 reasoning 兜底、不再猜。`parse` 原是闭包无法单测，提为 `parseJudgeVerdict`。

**6) README 口径与实测对齐**
「对标评分双通道（真实分）」名不副实：LLM 评委给的是模型意见，未配 `judgeModel` 时评委与
写手同源同 Key 池属闭环自评（`docs/judge-anchor-calibration.md` 自证人工认可稿仍被打 62~90、
单模型方差 29）。诚实声明与后文「18 个官方送检点」原本站不住，现统一为"换算不可靠"并给出
依据：`scripts/calib-sanity.ts` 实测 **14/18 点 x 轴漂移**（其自身口径为">3 即校准线过时信号"），
最凶 6 点是"数据源 x=0 → 当前引擎 60"，即 v0.9.10 清算的 `Number(null)===0` 塌缩锚点污染。

**已知问题（本版未修，需要成本或标定决策）**

- `strictFidelity` 默认 false → 编造复核默认关着；质检通道异常时记 `pass` 放行（已留痕）。
- 多网关合议庭代码完整，但 `setJudgeSeats` 只被 `scripts/` 实验脚本调用，App 内恒为单裁判；
  席间分歧度 `spread` 算出后无人读取。接进生产 ≈ 三倍评分调用，属成本决策。
- `calib-sanity` 只打印不拦（退出码 0）；`regression-12samples.ts` 在提交态就是红的
  （A 3/12、B 9/12），其中 A 套件是"旧引擎 processedText × 新 aiScore"、B 失败 3 行全是未
  处理原文，属评分器漂移而非引擎退化。未重生成基线——那等于把当前输出当真值。
- 最后一次真实官方送检停在 2026-09-13；v0.9.9 之后没有新的官方数据。
- 被动句式（被广泛使用于/受到广泛关注/被认为是）不在 `aiScore` 计分表内：一段堆满这类套话的
  AI 稿被判 0 分（"完全人写"），被动软化的收益本地指标看不见。

## v0.9.11 更新（元压测闸门 + 谓语位塌句清算 + 被动软化 + 字符级对照）

**背景**：把旧优化工作区里那份从未进过版本库的压测脚本移植为常驻闸门（`src/engine/meta-stress.test.ts`，
A–J 十维），再用它扫引擎。第一遍报出 357 处失败，其中 **286 处是断言口径过期而非引擎坏**——
200 处签名对语料的**输入原文**自带内容恒命中（"，予以了反馈"），86 处全是 `injectSelfQA` 故意造的
"意味着什么？"。口径修正后，三个真缺陷才露出来。移植旧脚手架时同样先做了这件事：旧副本里
`apiKeys` 丢失、切句三重守卫、judge 锚定三条"缺失"，正本其实早已各自修好。

**谓语位盲删塌句（本版本最严重）**

- `stripAICliches` 原来是 `out.split(c).join("")` —— 对 `MECH_CLICHES` 无边界全局盲删。但那 37 条里
  混着一批**谓语短语**（展望未来 / 按下了快进键 / 具有里程碑意义 / 做出了不可磨灭的贡献…），
  它们往往是所在小句唯一的谓语，删完只剩光杆主语，再被垫词补成"人工智能技术吧。"的废句。
  实测有替身兜着的条目 3/100 塌，没替身的 **92~100/100 必塌**。
- 改为**只删小句起始位**，判据与同文件 `stripLeadingConnectivesHard` 同源。37 条谓语位审计：塌句 **0**。
- 谓语位不能删的，走新增的 `MECH_CLICHE_REWRITE` 口语等价物改写。强度 1.0 总均分 **0.75**，
  优于盲删时代的 0.80 —— 即没有用任何语法正确性去换分数。
- 为什么此前一直没人发现：见文末"目标函数盲区"。

**形式动词语槽守卫**

- `进行/予以/加以（+了）` 之后只能站动词性名词；`优化→调好` 撞进"进行了优化"会产出"进行了调好"，
  元压测 A 维 **188/1800** 命中。加尾锚定守卫（`humanize-guard.ts`），并在 A 维留下永久回归签名。
- 摘掉 `逐步→陆陆续续` 替身（71/1800 搭配病句）：逐步是有意分阶段，陆陆续续零散且多用于复数主体。

**被动软化 softenPassive**

- 按 `docs/history/quaiwei_句法多样性v058` 的报告规格实现（该函数在全机器任何代码副本里从未存在过，
  只有报告描述，所以这是新写不是移植）。五条固定搭配整块替换，**必须排在 `replaceVocab` 之前**：
  否则"受到广泛关注"的"关注"先被换成"留意"，规则永不可达（实测退化产物正是"受到广泛留意"）。
- 配套把守卫前窗 3→6 字并加 `受到/得到/予以 + 关注·重视` 特判。加窗安全是因为守卫的前缀判据
  全部尾锚定（`endsWith` / `/…$/`），多给几个字不改变任何既有行为。

**对比面板降到字符级**

- `DiffView` 此前整句标红/标绿，看不出"到底改了哪个字"。句内裁切的 `trimCommon` 早就有但无人调用，
  且它只能裁出**一段**连续变更——同句两处改动会被并成一大块。新增 `charParts`（逐字符 LCS，
  `Uint32Array` 扁平表）+ `diffInline` + `diffStats`；单边超 1500 字退回 `trimCommon`，
  因为 O(m×n) 再往上会卡主线程。标题改为「改动了 N 处 · R/T 字（P%）」。

**校准读取收敛（numOrNull）**

- `Number(null)===0`、`Number("")===0`、`Number([])===0`、`Number(true)===1` 这一族"非数字的值被静默
  当成 0/1"的入口，在 detector / calib-lab / api-zhuque 三处独立踩到，本次收敛为 `engine/zhuque.ts`
  导出的唯一实现。旧版校准点的读取同样收成单一函数：过去两处各写一份，同一个缺陷只被修掉一处，
  而写回侧会把伪观测点**永久固化**。

**工程**

- `electron-app/index.html` 真正移出 git 跟踪：`.gitignore` 第 11 行的规则此前一直空转
  （gitignore 对已跟踪文件不生效），导致每次构建都脏工作区、只能靠专门的 chore 提交收尾。
- 仓库从 `D:\deep\quaiwei` 迁至 `D:\projects\quaiwei`，Key 轮换文档里的操作路径同步修正。

**已知问题与目标函数盲区（未修，留给后续判断）**

- **`aiScore` 看不见语法塌缩**：句子被改写到只剩光杆主语时仍给 0 分，`verify-quality` 也判"零泄漏"
  通过 —— 所有指标只奖励"套话删掉了"。所以"分数变低"不能当作"改写变好"的证据，质量判断必须同时
  看句子还成不成句。同理，一段堆满"被广泛使用于/受到广泛关注/被认为是"的 AI 稿 `aiScore` 给 **0 分**
  （被动句式根本不在计分表里），软化后反而 30~60：被动软化的收益本地指标看不见。
- `aiScore` 的"机器/真人"分界 29 只硬编码在 `scripts/_calib_margin.ts`、`_pad_legit.ts` 两个
  gitignore 的草稿脚本里，引擎侧没有导出常量。
- `scripts/regression-12samples.ts` 在已提交状态上就是红的（A 3/12、B 9/12）。A 套件比的是
  "旧引擎存的 processedText × v0.8+ 重标定后的 aiScore"，B 套件失败 3 行全是未处理原文，
  属评分器漂移而非引擎退化；lock 里一批 `old=0` 正是上面那族数值塌缩污染。未重生成基线。

## v0.9.10 更新（静默错误清算 + 节奏基线 + 工程卫生）

**背景**：一次针对「**失败方向最危险的那类 bug**」的系统排查——不是"算错"，而是
"坏数据被静默读成看起来合理的好值"。这类 bug 的共同点是：UI 上完全看不出异常
（校准点照常增加、评分照常返回、测试照常通过），但下游结论已被系统性带偏。

**数值塌缩清算（四个入口，同一病根 `Number(null) === 0`）**

- `zhuque.ts#parseOfficialResult`：官方三段占比 "人工特征 0.01% 疑似AI辅助 0% AI生成 99.99%"
  旧逻辑先按优先级定为 ai 档，再取**文本里第一个**百分比 → `label=ai, probability=0.01`。
  这种自相矛盾的点写进校准库会把映射线彻底带偏。改为**档位与概率同源**：命中哪个档位词，
  就取该档位词之后的百分比。另补前置守卫 `(?<![\d.-])`，避免把 "-5%" 读成 5%、
  从小数片段里抠出看似合理的分数。
- `calib-lab.ts`：新增 `numOrNull()`，空值保持"缺失"而不是塌缩成 0（0 在评分域里是
  **真实值** = 完全人类）。半损坏样本不再伪装成 (local=0, official=99) 的观测点拽歪拟合线。
- `detector.ts`：`getPath` 中途断裂显式返回 `undefined`，否则检测器故障被读成
  「0 分 = 完全人类」——失败方向最危险的一种误判。
- `ppl/scorer-core.ts`：抽出 `toNumberList()` 归一 transformers.js 张量（tolist/data/嵌套数组
  三种形状），归一失败返回 null 让上层抛错；静默拿到空数组会算出假性 0 分。

**句长节奏过平：基线化（`scripts/scan-bugs.ts`）**

- 「句长节奏过平」从违规改为**版本库基线**（`RHYTHM_BASELINE = 31`）：命中 ≤ 基线通过，
  超出即报警。`SCAN_UPDATE_RHYTHM=1` 可打印最新建议值。维护语义与
  `regression-12samples.lock.json` 一致。
- 为什么建基线而不是修：CV 偏低是「v0.9.6 证伪 CV 判别力 → v0.9.8 废除语气锚注入」的
  **必要代价**。引擎只剩 `boostBurstinessByCutting`（纯切长句、零注入），短文本切句空间
  不足，0.9 档 CV 稳定落在 0.19~0.37（阈值 0.45）。修它 = 重新引入语气词污染（新标尺
  第一损伤源，单剥可回收 33~84 分）。
- 配套：`MIN_BURSTINESS_CV` 0.45 → 0.40，反映废除极短语气锚后引擎的真实能力。
  0.45 是旧语气锚引擎时代标定的；朱雀官方口径的绝对判据（std∈[4.5,8.5]）已在
  `fingerprintCheck` 独立检查，CV 是补充判据。
- **注意**：基线与 `MIN_BURSTINESS_CV` 必须**同批提交**——基线值是按新阈值标定的，
  只提交基线会得到红的回归（实测提交态 v52=32）。

**v5.2 外部样本病句守卫（scan-bugs 违规的真实修复）**

- `humanize-guard.ts`：新增「的+V」名词位守卫。"实现的突破"这类名物化定语里替换动词会产出
  "的弄成的突破"式病句（v5.2「名词位替身」违规根因）。
- `humanize-shuffle.ts`：`clampAvgSentencesInBlock` 切句前用 `fragmentCanStand` 检查后半句
  能否独立成句——"减轻教师的负担，让教师将…"切成"。让教师将…"是无主病句
  （v5.2「使役无主句」违规根因）；`narrative/humanHand` 体裁级跳过全部自问自答
  （"例子呢？"不属于叙事/人写原稿）。
- `capParticleSentenceDensity` 空壳段判据收紧：原「全段句子均 ≤8 字」会把正常短句段整段清空
  （"这个功能确实好用。"正好 8 字，是常见中文句长），实测直接把该类段落返回成空串。
  真特征是「语气词残留 + 全段极短」，故改为：≥2 句、均 ≤5 字、且至少含 1 条独立语气句。

**Key 解析统一（`llm-config.ts`）**

- 抽出 `splitKeys()` 作为全局唯一实现：支持换行/半角逗号分号/**全角逗号分号**，去空白去重保序。
  设置面板是中文界面，用户粘贴带「，」「；」是常态，只认半角会静默吞掉第二个之后的 Key
  （表现：填了 3 个 Key 却照样 429）。`multi-roles.ts` 读本地 Key 文件也改走这里，避免两套规则漂移。

**工程卫生**

- 覆盖率门禁 `npm run test:cov`（lines/statements ≥75）+ `coverage/` 入 gitignore；
  lint 范围从 `src/` 扩到 `src/ scripts/`；CI 拆分可选依赖安装，构建不再被
  23MB ONNX wasm 拖慢。
- 新增 4 个测试文件（detector 15 例 / multi-roles 10 例 / zhuque 37 例 / shuffle-guards 20 例），
  全部锁「静默错误」路径；测试规模 425 → 548 例。

## v0.9.9 更新（多网关合议庭 + 修订层"未改进即停"守卫移植）

**背景**：2026-09-12 真实 LLM 闭环实验（3 篇样文 × 三轮修订）暴露两个问题：
① 单裁判判别力不足——glm-5.2 与 deepseek-v4-pro 对人工评审通过的稿子打 62–90 高分，
且单模型采样方差 29 分，「换宽评模型」经对照实验证伪；② 修订层缺少守卫——
第 2 轮修订把句子碎句化，触发表演性人味新指纹，分数 75→88 不降反升。

**多网关合议庭（`judge-panel.ts` + `multi-roles.ts`）**

- 三席跨网关独立评审（sensenova deepseek-v4-pro / amd DeepSeek-V4-Flash / nvidia glm-5.3-flash），
  不同推理栈偏置不相关；
- **痕迹语义归一 + ≥2 席交叉定罪**：单席孤证（模型偏置/抽风）降权，两票重合才进修订清单——
  实测从"一个分数"变成"可执行修订清单"（虚构人物事例/对仗工整/结尾模板化等精确靶子）；
- 加权中位数聚合 + 席间分歧度可见化；席位故障不连坐（单席失败少一票，全挂才回退单裁判）；
- 真实验证：3 篇 × 3 席全部成功，席间分歧 5–20 分，痕迹重合度即修订优先级。

**修订层"未改进即停"守卫移植（`llm-humanize.ts`，v0.9.6 引擎侧同款思路）**

- **L1 痕迹收敛守卫**：修订轮交叉定罪与上一轮重合 ≥2 项 → 定向修订无效，立即停止
  （实测：痕迹可精确消除，但总分被句长/收尾类风格噪声托底，75→75 不动——分数单指标看不出，必须比痕迹集合）；
- **L2 风格回退守卫**：修订候选引入上一轮最优稿没有的本地硬指纹（碎句化修订把节奏
  拉回 AI 特征带）→ 拒收本轮、回滚底稿并停止（风格回退不是下一轮能修好的）；
- 实验对照：无守卫时第 2 轮 75→88（过度修订），守卫语义对应实验数据已录入
  `artifacts/llm-closed-loop/panel-revision-*.md`。

**通道基建**：AMD 网关（DeepSeek-V4-Flash，1.0s）、NVIDIA 网关（glm-5.3-flash，2.7s；
glm-5.2 已 410 下线）接入；OpenRouter ox-alpha 404 弃用（Key 402 余额不足）。
Key 全部走 gitignored 本地文件（`endpoints.local.json`）。

**兼容性**：未配置合议庭席位时行为与 v0.9.5 完全一致（`processCandidate` 自动回退单裁判）。


## v0.9.6 更新（注入器与标尺的冲突清算 + 默认风格转向）

**背景**：一次"引擎自身制造机器味"的系统性排查。起因是重新摘录标定语料时发现两条
`@ENGINE-REAL` 样本（论说 89 分、叙事 62 分）已无法复现 —— 深挖后发现真实原因不是
引擎变好了，而是**引擎的去味手法与标尺的判定对象直接冲突**：引擎靠"往文本里塞口语垫词"
来制造人味，而标尺把垫词判为机器味。等于左手拆、右手砌。

**默认风格 casual → plain（核心决策）**

实测（4 组真实 AI 文本 × 4 档 × 20 seed = 320 次）：

| 文本 | 原文分 | casual 通过/均值/最高 | plain 通过/均值/最高 |
|---|---|---|---|
| 公众号议论文 | 39 | 69/80 · 14.7 / **38** | **80/80** · 11.7 / 20 |
| 工作总结 | 12 | 76/80 · 8.0 / **34** | **80/80** · 3.4 / 8 |
| 产品介绍 | 6 | 80/80 · 2.9 / 24 | **80/80** · 1.4 / 6 |
| 议论文长文 | 18 | 76/80 · 4.3 / **40** | **80/80** · 0.8 / 8 |

- 反直觉结论：**plain 不是"去味更弱"而是"去味更强"**。原假设"casual 更口语更像人"
  不成立，因为引擎的"口语化"就是塞垫词，而垫词正是标尺判定的机器味。
- 交叉验证：casual 独有四个注入器，污染上界 100/56/40；plain 对应 56/30/16。
- 另验证 casual 在对话/叙事/人写三体裁上与 plain 完全同分（本就被 `skipSelfQA` 门控），
  即 casual **没有任何一个体裁上的量化优势**。
- `DEFAULT_API.style`、6 处引擎兜底、设置面板文案同步调整；老用户已存的 `casual`
  配置保留不变（尊重显式选择）。

**替身池清洗（VOCAB 10 个问题条目）**

全表审计（`scripts/_vocab_audit.ts`）发现 10 个替身"插到句首即制造污染"，
根因是替身池取材于口语垫词，而垫词是标尺 3e/3d 的判定对象：

| 原文套话 | 旧替身（污染） | 新替身（安全） |
|---|---|---|
| 值得注意的是 | 要我说 | 说起来 / 顺带一提 / 有个细节 |
| 事实上 | 其实 / 说实话 / 讲真 | 实际上 / 平心来说 / 论实际 |
| 换言之 | 说白了 | 意思就是说 |
| 简而言之 | 说白了 | 一句话概括 / 用一句话讲 |
| 毋庸讳言 | 不瞒你说 / 实话讲 / 说白了 | 毋庸回避 / 这话得直说 / 避不开的是 |
| 一言以蔽之 | 说白了 | 拢共一句 / 合起来说 / 归到一处 |
| 综上所述 / 总而言之 / 本质上 / 归根结底 | 说到底 | 总的来看 / 归结起来 / 根子上 |

清洗后审计结果为 **0 个替身制造污染**。

**句尾语气词剔除「哈」**

`SOFT_ENDINGS` 原为 `["吧","呢","嘛","呀","哈"]`，但标尺 3a 的句尾语气词表
`[嗯啊哦嗨咳呣啧诶哈]` **只命中「哈」**，其余四字不罚。五字等概率抽取 = 20% 踩雷率。
实测剔除后「标准 AI 议论文 + plain」的最高分从 44 降至 30，句式分从 16 归零。

**plain 自问自答走克制池（注释与代码脱节的修复）**

`injectSelfQA` 早就为 plain 准备了 6 条克制型模板（「为什么呢？原因其实不复杂。」），
但外层调用条件写的是 `isCasual`，plain 根本进不来 —— 那组模板从未被使用。
修正为 `style !== "academic"`（academic 由空池天然禁用），plain 用自己的克制池。

**标定线重建（17 样本 + 8 验证样本）**

- M6（旧 `ENGINE-REAL-论说0.9档` 89 分 → 不可复现）：按纪律从当前引擎实测输出重新摘录，
  新样本为 casual 0.7 档 44 分，是当前可复现的**最高**论说污染。
- M7（旧 `ENGINE-REAL-叙事0.9档` 62 分 → 叙事已归零）：240 次运行全部 0 分。
  条目保留为**回归基准**（若叙事重新产出 ≥29 分应立即报警），证据体裁改为 `main`。
- 结果：训练集 0 错判 / 验证集 0 错判 / 合并 25 样本 0 错判，分离间隙 `[27, 30]`。

**验证汇总**

| 指标 | 结果 |
|---|---|
| 单元测试 | 419/419（35 文件） |
| tsc | 零错误 |
| 去味成功率（默认档） | 4/4 文本 80/80，最高分 ≤20 |
| 污染上界（原文已污染） | 垫词堆叠 升高 0/80；连接词堆叠 升高 0/80、最高 0 |
| 叙事/人写零劣化 | 最高 0，零劣化 |
| 红线 1 去套话 | 4→0 / 1→0 / 5→1 |
| 红线 2 标定分离 | 真人 max 27，污染 min 30，零错判 |

**注释卫生**：清理 `humanize.ts` 中三处重复堆积的收敛注释；合并两个条件完全相同的 `if` 块。

## v0.9 更新（反「新指纹」层 + 标定工程化）

**反「新指纹」层（`src/engine/anti-fingerprint.ts`）**——引擎注入的反检测特征本身
会变成新 AI 指纹（v0.8.6 glm-5.2 批判点：刻意口语化、模板复读、模仿痕迹），做四件确定性收口：

- **模板复读封顶**：自问自答/插话模板（"例子呢？我随便举一个你就懂了"等）全文每种限 1 次，
  容忍注入环节在中间插语气词导致完整模板失配（核心短语级去重）；全文模板总量 ≤4（扎堆抑制，
  真人不会连珠炮式抛插话）；
- **语体门控**：论说/学术（formal）下还原过度口语替换（"快速→麻利"→"快速"、"总的来瞅"→"总的来说"），
  双连接词叠放（"我感觉，总的来瞅，"）拆成一个（不限语体）；
- **残句开头守卫**：句首独词连接词（并/而/且/但/亦/另）引导的残句修复；合并逻辑不吞并独立
  语气短句（它们是句长 CV 的来源）；
- **场景块行保护**：剧本【场景/人物/背景…】块头行在逐句注入与节奏兜底（boostBurstiness /
  boostBurstinessIfLow）环节全跳过——修复"【场景…】啧。嗯。呣。"污染；
- **Bug 修复**：`collapseDoubleConnectives` 正则全为非捕获组却引用 `$1` → 输出字面"$1，"
  （叙事 0.7 实测触发）；破折号后跟句末标点/双逗号等模板删除残留的坏标点清理；
- **CV 顺序修正**：`capParticleSentenceDensity`（丢弃极短语气句）移到 boost 兜底之前，
  解决对话 0.9 档"指纹-句长节奏过平"；
- 新增 13 个单元测试（含 $1 回归锚点、扎堆抑制、语气短句豁免），全量回归零违规。

**标定工程化**（单一数据源，详见 `scripts/calibration-data.json` 与 docs §3.0）：

- 18 个官方送检点 + 四体裁线参数 + 每点文本重建参数收敛为**单一数据源**；
- `scripts/calib-sanity.ts` 三重自检（参数一致性/锚点误差 ≤8pp/x 轴漂移报告），接入 CI；
- `zhuque.test.ts` 锚点守卫升级：12 点硬编码 → 数据源驱动 18 点全量 + 参数同步校验；
- **漂移体检发现**：论说线 3 个归档点全部漂移（原文 aiScore 33→92），叙事/对话/人写零漂移
  ——论说线刻度已过时，待 v4 官方送检数据重拟合。

### v0.9 去味质量与忠实度收口（第三方独立评测驱动，2026-09-09 ~ 09-12）

以不使用本项目自带 `detectAI` 的独立指标（套话密度、句长/段长变异系数、TTR、术语完整性、
标点指纹）对本地引擎与 LLM 通道做全通道实测，据实测结果收口。完整报告见
`artifacts/去AI味评测报告.html`。

**本地引擎（4 项 P0 + 1 项 P1）**

- **中英/中数空格**：`stripCJKEdgeSpaces` 原无条件剥离，技术文 75 处空格全删
  （"从 Webpack 迁移到 Vite"→"从Webpack迁移到Vite"），且强度 0.3 就触发。改为跟随原文排版；
  新增 `HumanizeOptions.stripCJKSpaces`（默认 false），旧剥离能力由 `scan-bugs` 探针显式开启保留。
- **术语被通用词表破坏**：扩充 `GUARD_BEFORE`/`GUARD_AFTER` 复合词守卫 + 新增混排安全网
  （命中词紧贴 ASCII 标识符/数字/代码符号即跳过）；收敛 `基于`（去"靠着/拿"）、`构建`（去"建"）。
  技术文术语完整度 12/17 → 16/17，6 个 seed 一致。
- **句序破坏因果**：`SEQUENCE_HEAD_RE` 补序号/因果锚词（一是/因为/首先…）。
- **垫词句中注入**：新增 `LOGIC_ANCHOR_HEAD_RE`，逻辑锚点处不插碎片，被拦时顺延下一安全位
  （而非放弃，避免句长方差下降）。
- **叠字/残字**：`replaceVocab` 加叠字守卫（"彰显着"→"透着着"、"不仅是"→"不只是是"）。

**LLM 通道**

- **深度闭环空响应中断**：新增 `chatNonEmpty()` 降级链（默认 → token 预算翻倍 → 换备选模型 →
  退回纯原文重改写）。此前第 2 轮空响应即 break，158~242s 后静默交付未达标稿；现可跑满 2 轮。
- **首轮多候选竞争**：新增 `ApiConfig.contestSamples`（默认 1 = 关闭），未配 `altModel` 时
  同模型采样 N 稿取最低分。依据：实测改写稿质量在 20~88 分间横跳（极差 89），
  而评判尺子极稳（同文本重复评判极差仅 2）——瓶颈是采样运气，不是评判。
- **未改进即停**：以 `bestScore` 为基线，本轮不优于当前最优即收手（初值 999 哨兵不误停）。
- **锚点修正**：竞争段多分时 `anchorScore` 取最优分而非首个分。
- **收场原因可见化**：未达标时 `note` 明确写出「仅完成 N 轮，未达目标 ≤X，结果可能仍偏 AI」。

**编造防线（改写工具的生命线）**

- **根因**：`SYSTEM_PROMPT` 自相矛盾——第 11 条"抽象概括**必须**落到具体"逼模型举例，
  第 8 条"加入第一人称（按我的经验）"诱导编造经历，第 16 条"严禁虚构"被两者对冲。
  实测输出出现「我舅体检确诊肺癌」「邻居孩子三个月提分」「公司招人月薪八千」，字数膨胀 32%。
- 改第 8/11/16 条口径 + 铁律加"字数 ≤ 原文 110%"。复测：747→541 字，**零编造**，文体保持议论文。
- **质检显式化**：编造单列为独立检查项并显式枚举（人物/亲属/经历/场景/机构/城市/工资/年份），
  要求逐句比对而非只扫数字术语。
- **本地零成本兜底**：新增 `fabricationIssues()` 并入 `localHardGate`，
  口径"改写稿有且原文无"才判定，不误伤真人原稿。

**两通道空格统一**

- LLM 侧 `SYSTEM_PROMPT` 第 18 条、academic 文风、轻度模式指令三处改为跟随原文排版。
- 提示词管不住（英文术语旁照删）→ 新增 `restoreMixedSpacing()` 确定性后处理，
  单轮与深度闭环均覆盖。仅当原文带空格排版时回填；只在「汉字 ↔ 字母/数字」边界插入
  （初版把中文标点算进去，产出"， 1200 个模块"这类错误空格，已修正并补单测）。

**测试**：355 → **375** 通过（新增 20 条）。`scripts/verify-quality.ts` 通过，
`scripts/scan-bugs.ts` 违规 37 → 6（剩余为"句长节奏过平"，无版本库基线可比对）。

**设置面板**：新增「首轮候选数」下拉（1/2/3）；「调用上限」补提示——
每轮约 5~6 次调用，跑满 4 轮需 20~24 次。

## v0.8.8 更新（评测驱动：真实 LLM 闭环实测暴露的问题收口）

用 `scripts/_real-eval-local.ts`（3 体裁 × 3 档 × 本地引擎）与 `_real-eval-llm.ts`
（SenseNova 真实 Key 池 × 深度闭环）做全链路实测，修复实测暴露的三处问题：

- **Bug 修复：ESM 下 Key 池静默丢失**——`loadPresetKeys` 原用
  `new Function("return require")` 取 fs，`"type":"module"` 下 require 未定义被
  catch 吞掉 → Node 侧脚本永远拿不到 `scripts/.sensenova-keys`，LLM 通道在
  脚本场景静默失效（Key 文件明明存在却报"未配置 API Key"）。改用
  `process.getBuiltinModule("node:fs")`（Node ≥22.3，同步、对打包器不可见、
  浏览器构建零影响），CJS 走 require 兜底；新增 `llm-config.presetkeys.test.ts`
  锁死回归。顺带收敛双实现：删除零调用路径 `node-keys.ts`，
  `scripts/api-test.ts` 统一走 `loadPresetKeys`。
- **Bug 修复：「取得了」→「得了了」病句**——词表替身"得了"尾带"了"，与原文
  尾随"了"拼成病句（多种子稳定复现）。仿「发挥着/发挥了」惯例注册长搭配
  `取得了: ["拿到了","有了","见到了"]`，裸"取得"删掉"得了"；质量回归新增
  20 种子断言（无「了了」/无「得了」）。
- **评判锚点（宽严自适应）**——绝对目标 10 按宽评评判员标定，实测 glm-5.2
  交叉评判对双模型竞争胜出稿仍打 86/91 → 永不达标、白烧轮次靠预算收场。
  现以首个有效正分为宽严锚点：达标线 = max(10, 首轮分 × 0.35)，全程只锚一次
  防"越改越宽"，放宽写入 note 与 `DeepResult.targetUsed`。严评（首轮 86）→
  ≤30，宽评（首轮 45）→ ≤16，首轮已很低则维持绝对目标。单测 2 例锁死。
- **语气词密度收口**——多注入器叠加后同段可出现「呣。哦。咳。」三连独立极短句
  （单个都在守卫限额内，叠起来仍是"过度人味"机器指纹，v0.8.6 质检提示词已列
  FAIL）。管线出口新增 `capParticleSentenceDensity`（仅 ≥0.75 强度档启用，
  低强度档极短句是句长 burstiness 的主要来源）：句尾挂词 + 独立极短语气句
  合计每段 ≤2，超额剥词保句/整句删除（确定性、幂等、零随机）。
- **DX**：设置弹窗对以 / 开头的相对路径 Base URL 增加静态托管警告（仅 dev
  代理 / Electron 桌面版可用）；深度模式文案同步锚点语义；`使用说明.txt`
  重写为 UTF-8 并更新至 v0.8.8（旧文件是 GBK 乱码 + v0.5.1 旧内容）；
  electron-app 版本号同步 0.8.8；调试产物（artifacts/、_scan_out.txt）移出
  git 跟踪并补 .gitignore。
- 测试 343 → 345 例（+2 评判锚点；Key 池回归 3 + 质量回归 1 已随后一并入库）。

## v0.8.7 更新（测试补盲区收口 + DX 增强 + vite 6）

工程与测试四轮迭代（224→323 例，+99），高/中风险盲区全部清零：

- **测试补盲（四轮）**：
  - `store` 15 例（逐字段清洗/secureStore 桥接三态）+ `store-history` 8 例（去重/上限/配额降级）；
  - 组件全量：ScoreBadge/TextPane/DiffView/HistoryPanel/ErrorBoundary/FingerprintPanel 21 例、
    SettingsModal 9 例（草稿-提交/温度归一化/预设）、BenchmarkPanel 11 例（v3 体裁轨道联动）、
    CalibLabModal 启用态 5 例、ppl-client 集成 7 例、App 整树冒烟 9 例——11 个 UI 组件全覆盖；
  - scripts 渐进：词表卫生规则固化为断言、humanize-cli 纯逻辑（esbuild 转译 + vm 沙箱，
    零改动 CLI 行为）。
- **Bug 修复**：`store.ts` loadApi 漏读 `apiKeys`——用户保存的 Key 池刷新后静默丢失（测试驱动发现）。
- **DX 增强**：`llm-chat` 错误透传网关响应体（error/message/detail，截 200 字符），
  404/402 等错误能直接看到「模型不存在/配额用尽」等真实原因；一键启动 .bat 加 Node ≥18 门槛。
- **依赖升级**：vite 5.4→6.4.3（@vitejs/plugin-react 4.7→5.2），配置零改动兼容，主包 331KB。
- **P1 预置**：注入密度 A/B 实验材料（同原文同种子，0.9 档 vs 0.45 档两版送检稿），
  待朱雀送检解锁后两轮即可回填校准点并触发 main 线重拟合。
- 诚实标注暂缓：Word/公式格式保留、知网/维普多平台分策略（需产品决策）；朱雀真实送检
  受匿名验证链阻塞（连解多轮验证码仍 Invalid request，需登录态）。

## v0.8.6 更新（竞品对标：术语保护 + 批量 CLI + 语义层反指纹维度）

对照 2026 年竞品格局（言笔/笔灵/SpeedAI 等主打"术语零改动"，嘎嘎降AI/零感AI 标配批量处理，
文必过首创"目标 AI 率设定"）补齐三项差距：

- **中文术语保护清单**（`engine/term-protect.ts`）：内置 58 项跨学科术语（法学"善意取得"、
  医学"细胞凋亡"、计算机"机器学习"等），替换引擎在术语位置直接跳过；支持
  `setProtectedTerms()` 注入论文题目/人名/产品名等自定义术语。此前只有事后拦截
  （checkFidelityLocal 查英文术语/数字），现在有事前保护。测试 4 用例。
- **批量 CLI**（`scripts/humanize-cli.ts`）：`npx tsx scripts/humanize-cli.ts <dir> --out <dir>
  --intensity 0.9 --zhuque` 一条命令批量处理 .txt，输出降档对照，竞品标配能力补齐。
- **语义层第 7 维「反检测引擎指纹」**（`api/zhuque-semantic.ts` + `llm-quality.ts` +
  `llm-prompts.ts`）：glm-5.2 实测证明深度改写稿的"刻意口语对仗/自问自答模板/模仿露怯"
  是旧 6 维提示词的盲区——这些恰是引擎自己注入的特征。新维度命中后定向修法表自动
  附"降低人味浓度/拆口语对仗"映射。实测同一样本评分 45→58，痕迹指认更精准。
- **Key 安全改造**（`api/node-keys.ts`）：SenseNova Key 移出源码（环境变量
  `SENSENOVA_KEYS` 或 gitignore 的 `scripts/.sensenova-keys`），浏览器侧预置按钮无 Key 时
  置灰提示。**旧 Key 已随 git 历史泄露，请到商汤后台轮换。**
- **校准体系**：对话线按漂移后 aiScore 重拟合（0.8827x+26.5，R²=0.999）；12 个官方锚点
  完整性守卫进单测，引擎改动导致漂移时 CI 亮红。
- **回归基线**：scan-bugs v5.2/v6.0 WIP 探针 82 次违规全部修零（词表卫生+守卫），
  双档回归全绿。

## v0.8.5 更新（级联守卫复活 + 词表单一事实源 + 连贯性校验 + 调用预算）

- **级联静态守卫复活**：scan-bugs 的级联守卫还在读旧路径 humanize.ts（词表早已迁到
  humanize-vocab.ts），复活后立刻抓出 **19 处真实级联隐患**——替身本身又是另一个键，
  会被后续 pass 二次替换成不可控形态（"紧扣→围绕→钉在"同款）。19 处全部修掉：
  推动/促使/驱动/牵引/立足于/增强/落实/体现 等词条的替身改用非键词
  （如 推动→"推一把"、增强→"强化/变强/加把劲"、体现→"显出/透出/看得出来"），
  现 280 词条替身无一包含其他键。顺带修掉 bestOf 择优探针里
  `FingerprintIssue.level` 不存在的字段引用（指纹本就只报真问题，直接取 issues.length）。
- **检测词表单一事实源**：新增 `engine/zhuque-lexicon.ts`，把 zhuque.ts 与 detector.ts
  各自维护且已漂移的 FORMULAIC/OFFICIAL/SKELETON/CONNECTIVES 四套词表收拢为并集。
  验证：合并后 calibrate-detector 可分性（AI 39 / 真人 21）与样本 D 综合分（60.22）
  **与合并前完全一致**——零分数漂移，纯防漂移收益。
- **连贯性校验**（`coherenceIssues`）：句序重排/段落重切最典型的两类断裂——
  配对衔接词失散（有「其次」没「首先」）与开篇孤代词——确定性检查并入 localHardGate
  喂修复轮；LLM 质检提示词同步补第 3 项「段落衔接」检查。
- **深度闭环调用预算**：`ApiConfig.maxApiCalls`（0=不限）+ 设置弹窗「调用上限」下拉。
  llm-chat 内置逻辑调用计数（重试/换 Key 不重复计），轮间检查、轮内不中断，
  超预算带当前最优结果收场，note 注明实际次数——时间预算之外的计费保护。
- **测试**：185 用例（+5：连贯性 4 + 预算 1）。

## v0.8.4 更新（本地引擎输出质量：反堆叠 / 反复读 / 词表守卫补漏）

对照第一轮实测暴露的三类硬伤（高强度档输出"呣诶嗨咳"堆叠、"是啊。是啊。"复读、
"借对海量数据/对口味学习方案"语义损伤），全部修掉，并新增 `humanize-quality.test.ts`
6 个质量回归用例（30 种子扫描）锁死：

- **锚点反堆叠**（`boostBurstinessInBlock`）：CV 追不上目标时老实现对同一句反复塞
  极短锚，拼出「行对哦诶啊嗯呵呣啧」粒子串。现改为：句尾已有锚点/语气词的句子跳过、
  锚点同块去重、每块总量封顶（2~4 条）。
- **极短句反复读**：管线里 `boostBurstinessIfLow` 会被调用多次（朱雀增强 + P5 清尾），
  每次调用的去重状态互不可见，跨调用仍会挂出「哦。哦。」「是啊。是啊。」。
  现尾部挂锚前实测文本尾 + `boostBurstinessIfLow` 出口兜底清扫相邻同串。
- **词表守卫补漏**（`humanize-guard.ts`）：`通过对N` 的"对"是介词，替身接上去必出
  "借对/靠对"病句——`通过` 后窗口见「对」即跳过；`个性化+方案/学习` 类错配同理。
  `个性化` 替身从「对口味/按个人来」换成「定制化/因人而异的」。
- **反编造对齐**：经验锚点里的「我身边有个朋友…」轶事与 LLM 提示词铁律
  （严禁虚构朋友轶事——检测员重点抓的破绽）自相矛盾，改为第一人称亲历表述。
- **碎片注入去重封顶**（`injectFragments`）：同碎片不再复读、总量约每 3 段最多 1 条。
- **附带收益**：本地引擎改动顺带修复了 scan-bugs WIP 探针 20 处 v5.2 违规
  （546→526），既有稳定探针保持全零。

## v0.8.3 更新（LLM 去味对齐朱雀检测员 + 本地硬门槛 + 表层双判据）

- **深度闭环收敛目标 = 朱雀检测员**：`llm-quality.processCandidate` 的评分阶段改走
  `ZHUQUE_DETECT_SYSTEM`（朱雀语义层提示词）——此前闭环用通用检测员提示词评分，
  优化目标与朱雀抓法脱节；现在"改什么"和"查什么"是同一把尺子，评判痕迹也是篇章层，
  修订轮定向更准。
- **本地零成本硬门槛**：新增 `localHardGate`（指纹体检 + 忠实度校验），LLM 质检放行后
  再过一遍确定性检查，垫词复读/中英空格/破折号超标/数字篡改直接打回修复；
  两条统计型节奏指纹（CV 过平/标准差带）刻意排除在门槛外，避免改写稿被反复打回空烧 API。
- **提示词升级**：主提示词补篇章层 4 条（指代落地/真实取舍/抽象落地/开头结尾反套路）+
  语气词整篇 ≤6 的反表演条目；修订轮按痕迹清单自动附「篇章层定向修法」
  （骨架→重切段落、泛指→落地、并列→加取舍、抽象→给细节等 6 条映射）。
- **朱雀语义层补第 6 维「表演性人味」**：官方对深度改写稿仍实测给 98.47%（样本 D 去味稿），
  抓的正是编造轶事/语气词堆叠/刻意碎句这类伪装痕迹；语义层提示词同步收紧打分口径
  （只做表层润色的改写稿 85+）。
- **朱雀表层句长节奏双判据**：`detectZhuque` 句长节奏维度并入句长标准差绝对判据
  （std∈[4.5, 8.5] 视为落入 AI 特征带，官方口径，与 fingerprintCheck 同阈值）——
  CV 达标但句长集中的文本不再漏判。
- **测试**：新增 `llm-quality.test.ts` 8 个用例（硬门槛打回/统计指纹排除/篇章修法映射/
  闭环走朱雀提示词）+ 句长标准差带用例，全套 174 通过。

## v0.8.2 更新（朱雀差距补全：语义层/困惑度/校准线打通）

对照真实朱雀的差距分析见 `docs/zhuque-gap-analysis.md`。本轮三项补全：

- **专用「朱雀检测员」语义层**：新增 `api/zhuque-semantic.ts` ——按官方实测抓法定制的篇章层
  提示词（论点骨架/指代链/因果推进/段落节奏/具体性 5 维审查），区别于通用检测员提示词；
  `llm-judge` 支持自定义系统提示词，交叉模型均值 / 单模型中位数聚合逻辑复用。
  朱雀面板「用 LLM 补语义层」即走此通道，来源标注「朱雀检测员提示词」。
- **困惑度层接入检测（第 13 维 · 隐层特征近似）**：`detectZhuque` 新增 `ppl` 选项——本地 ONNX
  伪困惑度按 ppl-calibrate 标定阈值归一（AI 组 meanNll∈[0.18,0.42] / 人工组∈[0.98,1.35]），
  以 0.15 保守权重并入综合分；困惑度模型就绪后朱雀面板自动重算并显示融合明细。
- **v3 体裁校准线打通**：BenchmarkPanel 的四体裁 18 点 OLS 校准表抽到 `engine/zhuque-calib.ts`
  共享；朱雀面板新增「体裁线预测」（本地 aiScore → 官方朱雀% ，R² 0.77~1.00）——
  无需攒校准点即有官方口径参考，两块面板从此共用同一把尺子。
- **测试**：新增困惑度层归一/融合公式、体裁线截断与体裁映射共 9 个用例，全套 131 通过。
- **语义层结果缓存**：`detectSemanticStable` 默认按「文本指纹（FNV-1a）+ 模型签名」缓存到
  localStorage（上限 50 条按时间淘汰）；同文本重复检测不再重复烧 2 次 LLM 调用，命中时来源
  标注「（缓存）」；「重跑语义层」按钮显式绕过缓存。
- **bench 新增 bestOf 档**：默认 8 稿择优路径进基准（571 字 13ms / 5710 字 106ms，交互无忧）。
- **Windows 桌面打包**：`electron-app` 下 `npm run dist`（electron-packager，--overwrite）产出
  `electron-dist/QuAiWei-win32-x64/QuAiWei.exe` 可分发目录，应用版本号与主包同步。

## v0.8.1 更新（深度吸收 C 盘副本：Key 池 / 多候选择优 / 桌面网关代理）

- **LLM Key 池轮换（v0.6.1 吸收）**：`ApiConfig.apiKeys` 多 Key 换行/逗号分隔，与 apiKey 合并去重；
  请求层 429/401/403 先换下一个 Key 立即重试，池耗尽再指数退避并从首个 Key 重来；
  kimi-k3 网关强制 temperature=1 自动适配。设置弹窗新增「Key 池」输入与
  「填入 SenseNova 常驻通道」一键预置（deepseek-v4-flash 主力 + glm-5.2 交叉评判 +
  deepseek-v4-pro 备选，2026-09-01 实测可用）；LLM 评判/语义层的可用性判定统一改走 Key 池。
- **bestOf 多候选择优（v0.6.0 吸收）**：新增 `engine/humanize-bestof.ts` ——一次跑多个随机种子
  各出一稿，三重硬门槛（忠实度不过 / 长度比出 0.6~1.4 / 高危指纹计数）淘汰，按
  「高危指纹 × 1000 + 本地分」排序择优；长文自动收敛候选数（>4000 字 4 稿、>1500 字 6 稿、
  其余 10 稿），全淘汰时兜底单次生成保证有输出。设置弹窗可开关并调候选数（1~30）；
  去味提示回显「N 稿中挑最优（淘汰 M 稿，中选种子 S）」。API 失败回退本地引擎时同样生效。
- **Electron 网关代理（v0.6.1 吸收）**：桌面版 main.js 内置本机转发服务
  （127.0.0.1:18964 起，端口冲突自动后移），把 `file:///sensenova/*` 请求重定向到
  token.sensenova.cn——解决网关 OPTIONS 预检 404 导致桌面版 LLM 通道必挂的问题；dev 仍走 vite 代理。
- **测试**：新增 `llm-keypool.test.ts`（Key 池合并去重/标点混排/空串）与
  `humanize-bestof.test.ts`（固定种子可复现、候选数夹取、忠实度与长度比门槛、选项透传），全套 122 用例通过。

## v0.8.0 更新（合并 C 盘桌面副本 v0.7.4 的检测器成果）

- **朱雀检测面板（本地近似 · 非官方）**：新增 `engine/zhuque.ts` ——对齐官方 6 个口径的本地近似器：
  三档占比（人工特征/疑似AI/AI特征，按字符加权）、AI 特征占比（官方核心指标）、12 维可解释特征、
  句级红/黄标注（悬停看命中原因）、官方通俗提示语、≥350 字门槛提示；
  支持「语义层（LLM 检测员）× 表层」双层融合（默认权重 0.8，可调）与本地分→官方分线性校准。
- **本地 AI 检测（14 特征三档）**：新增 `engine/detector.ts` ——词汇/句法/结构/衔接/人文/格式六层
  14 个统计特征，输出 AI 概率 + 三档判定 + 风险最高句子明细；阈值经 5 AI + 5 真人样本标定
  （AI 39~60 / 真人 9~21，安全可分）。去味完成后自动跑"原文 → 去味稿"降档对照。
- **官方送检与校准闭环**：新增 `api/zhuque.ts`（送检文本按句子边界截断、打开官方页、官方结果粘贴
  自动解析、校准点本地存储）与 `api/calib-lab.ts`（校准实验室：一篇原文批量生成 4 条样本 → 逐条送检
  回填官方结果 → 最小二乘拟合映射 → 网格搜索最优语义层权重 → n≥8 留出验证 MAE）。
- **UI**：新增「AI 检测」「朱雀检测」按钮与对应面板、校准实验室弹窗；去味完成提示新增
  本地检测降档与朱雀口径占比。
- **标定工具入库**：`scripts/calibrate-detector.ts`（正负样本特征分布标定）、`zhuque-smoke.ts`、
  `zhuque-truthcheck.ts`（样本 D 官方真值对照：本地 60.22 → 估官方 99.99%）、`calib-lab-smoke.ts`、
  `zhuque-semantic-live.ts`（统一改用 `npx tsx` 运行）。
- **测试**：新增 `zhuque.test.ts` / `detector.test.ts` 共 25 个用例（三档判定、占比归一、片段 offset
  还原、最小二乘校准、融合数学、官方结果解析、送检截断），全套 113 测试通过。

## v0.7.0 更新

- **Bug 修复（数据安全）**：深度模式在"预算耗尽且一轮都未完成"时会把**空串当去味结果**返回
  （用户输出被清空）——现抛错回退本地引擎，与单轮空输出守卫对齐；新增 API 层空输出回归测试
  （fetch 打桩，vitest 用例 16→18）
- **性能**：replaceVocab 单趟扫描收集命中+一次线性拼接、injectDialect 等价简化、4 处正则与
  连接词断句正则模块级预编译、guardBlocks 记忆化；x50（2.85 万字）基准稳定在 25ms
- **结构拆分（兼容桶模式，引用零改动）**：humanize-data.ts（999 行）→ 5 个领域模块；
  llm.ts（838 行）→ 8 个分域模块；App.tsx 抽取 FingerprintPanel/BenchmarkPanel 组件
- **状态优化**：输入/输出判空改 useMemo 派生布尔，消除每次按键的全串 trim 拷贝
- **健壮性/UX**：全局 ErrorBoundary（渲染异常不再白屏）；设置模态支持 Esc 关闭、补 dialog
  语义；温度输入改失焦归一化，修复清空重输时受控回弹跳值
- **安全**：CSP connect-src 收紧（去 http:）；secure-store 写入 Promise 链串行化防并发丢更；
  sync-dist 对源 HTML 已含 CSP 的情况输出告警

## v0.6.1 更新

- **Bug 修复**：修复 LLM 质检 `NOT PASS` 误判为通过、深度模式轮数前端与引擎不一致（"最多3轮"→
  统一为 `DEEP_MAX_ROUNDS=4`，导出常量可配置）、本地引擎缺失 `injectHalfWidth` 调用（半角逗号微混入
  现三处统一生效）
- **LLM 通道反指纹兜底**：新增 `crossChunkCleanup`——单轮 LLM 输出与长文分块拼接后统一过一遍
  全文级限额去重（垫词/破折号/省略号/段首过渡词/套话/空格），避免各块"各出现一次"的垫词在块
  边界累积成新指纹
- **数据词典清理**：删除"唯一替身=原词"的死项（`获得感`、`闭环` 自替身导致强度 1.0 时约 50%
  泄漏，verify-quality 可测出），检测价值转入 `FORMULAIC` 保留评分；修复 `DIALECT_VOCAB` 的 3 个
  替身内部重复（`里面`/`外面`/`思考`）——被新增的数据完整性 vitest 测试自动捕获
- **工程增强**：添加 `.gitignore`、扫描测试失败时 `process.exit(1)` 供 CI 感知（含
  `verify-quality`，其硬性泄漏门禁现真实生效）、`humanize.test.ts` 改为 vitest 真断言、
  启用 `noUnusedLocals`/`noUnusedParameters`、`test:quality`/`bench` 脚本、`.editorconfig`、
  GitHub Actions CI（类型/单测/回归/质量门禁/构建五关）、Electron 单实例锁 + 窗口打开/导航拦截、
  Electron 打包 `dist` 脚本、`vite.config.ts` 条件轮询（仅非 ASCII 路径启用）、`bench.ts` 导入风格统一
- **代码重构**：提取 `sentenceStats`/`computeStats`/`findSplitPoint`/`makeRng`；`SettingsModal`
  独立组件（草稿-提交模式）；`store.ts` 逐字段类型校验；`any` → `unknown` 收窄；`as Score`
  断言消除（统一用 `ScoreBreakdown`）；`CHUNK_THRESHOLD`/`CHUNK_SIZE`/`ZHUQUE_MIN_CHARS` 命名常量；
  删除死导出 `PUNCT_IRREGULARITIES`/`ELLIPSIS_ENDINGS`（早期规划数据，被确定性反指纹规则替代）
- **安全**：Electron 渲染进程 `sandbox: true` + CSP 头注入；`qualityCheck` 精确匹配防误判

## v0.6.0 更新

- **朱雀增强模式**：开启后叠加方言词汇、句中插入语、主观意见短语、括号自语、句式片段等反检测特征
- **v0.5.3/0.5.4 引擎提升**：强化副词+动词搭配修残（"非常提高"→"提高"）、AI 结论收束词替换（"这说明"→"说白了就是"）、让步句重构进入本地引擎、关于X的论述开篇去套话
- **代码结构优化**：数据词典拆分至 `humanize-data.ts`、预排序 VOCAB_ENTRIES、魔法数字→命名常量
- **测试升级**：`tsx` 替代 `--experimental-strip-types`、可选 `vitest`

