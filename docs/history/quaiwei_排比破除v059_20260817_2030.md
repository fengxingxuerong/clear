# 趣AI味 QuAiWei v0.5.9 排比/对仗破除 — 任务记录

日期：2026-08-17 晚
项目：`D:\_冗余归档\workbuddy会话\_portfolio_test_artifacts_20260807\unpacked\quaiwei\quaiwei`

## 目标
覆盖检测器核心抓点"结构规律性"的最后缺口：工整排比/对仗短句。

## 改动（src/engine/humanize.ts）

### breakParallelism（新函数，replaceVocab 之后，概率 0.5×强度）
1. **动宾三连**："要A，要B，要C" → "要A，还得B，也别忘了C" / "要A，也得B，还有C" / "要A，B也不能落下，C更得抓紧"
2. **介词排比**："在A中，在B中，在C中" → 第二项换"搁B"或"B这块"
3. **判断句三连**："X是A的，是B的，是C的"（第二三项承前省略主语）→ "X是A的，算B的，C也没得跑" / "B这块没得说，C也实打实" / "算B的，C也差不了"

## 关键踩坑
1. **edit 工具转义**：正则字面量里的 `\n` 被写成真实换行 → SyntaxError。改用 `\u000a`（纯 ASCII）。
2. **判断句形态**：AI 排比第二三项承前省略主语（"是B的，是C的"），初版正则要求三项都有主语匹配不到。
3. **回调参数错位**：正则捕获组与回调参数不对齐，输出 "undefined算..."，重数修正。
4. **残留代码**：edit 部分替换遗留旧回调 5 行 → 语法错误，手动清理。

## 验证
- opt-parallel.test.ts 8 样本×4 种子：三类排比破除生效、非排比对照不动、零病句
- scan-bugs 全量回归全绿（120+20+20+体检+审计+忠实度）
- opt-stress 250 次零病句/零残留

## 遗留
- 临时脚本累计 20 个待手动清理
- "需要A，需要B，需要C"三连未处理（语义风险，暂不冒险）
